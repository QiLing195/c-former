# C-Former 企业 AI 治理落地 POC 报告（toB）

日期：2026-09。定位：**C-Former 作为企业 RAG/LLM 的确定性治理层**——不是又一个知识库方案，而是给任何 AI 知识库装上"权限、诚实、留痕、边界"四根支柱。

## 0. 一句话主张

> 企业 AI 落地失败，不是因为模型不够聪明，而是因为**不敢信**：怕泄密、怕乱编、怕答错无据可查、怕触碰"文档之外的人情世故"。C-Former 用**确定性治理**解决"敢不敢信"——权限零泄漏、空白不编造、裁决全留痕、人际不越界。

## 1. 四支柱与验证证据链

### 支柱 A：权限闸门（防泄密）——检索前 mask，"原理上答不出"

| POC | 场景 | 结果 |
|---|---|---|
| [检索层](RAG_FUSION_POC.md)（`rag_permission_poc.py`） | 学生问 6 个敏感问题（目标=闭源） | 无闸门泄漏 **5/6（83%）** → 闸门后 **0** |
| [生成层](RAG_FUSION_POC.md)（`rag_permission_poc_llm.py`） | 同上 + DeepSeek API 生成 | 无闸门 LLM 泄漏 **4/6**（答出 GPT-5.2/Grok2026）→ 闸门后 **0**（全答"无法回答"） |
| 真实制度域（`eval_employee_rules.py`） | 员工管理制度（真实来源，9 问） | 无闸门内部泄漏 5 → 闸门后 **0**，3 条内部问法 DENIED |

**原理**：`mask = -inf; mask[visible] = scores[visible]`——不可见对象在检索**前**被排除，不进入候选集。无论神经分数多高、LLM 多会编，都看不到机密。对比事后过滤（答完再删），这是原理级保证。

### 支柱 B：字段级权限（细粒度）——三级角色分级

`eval_employee_rules_complex.py`（真实制度 + 三级角色 employee/manager/hr）：

| 测试 | 结果 |
|---|---|
| 三级权限分级生效 | **10/10** |
| 组合问法（请假流程+证明+批准） | 命中 ✅ |
| 含噪口语（"忘打卡算旷工吗""会不会被劝退"） | 命中 ✅ |
| 边界判断（"病假事假扣钱一样吗"跨条款） | 命中 ✅ |

**关键**："旷工会被劝退吗"——employee 只见"扣50元"（劝退标准不可见），manager 加复核权，HR 才见劝退标准。含内部细则的对象**公开部分可看、内部字段拒绝**（对象级 mask 会误杀，字段级不会）。

### 支柱 C：制度空白识别（诚实）——明文可答 vs 空白升级

`eval_rule_gap.py`（真实制度 12 问）：

| 类型 | 识别率 | 行为 |
|---|---|---|
| 明文可答（"迟到扣多少""病假要证明吗"） | **100%** | 正常回答 |
| 制度空白（**出差2天变5天**/补卡/自动续假/产检/申诉） | **100%** | 诚实判 GAP → **升级 HR**，生成升级记录 |
| 制度范围外（离职社保/加班费） | **100%** | 升级 HR |

**核心价值**：裸 RAG 会为"出差超期"胡诌一条"根据制度第X条…"；C-Former 识别"命中条款但不含答案"→ 明确"制度未覆盖，请咨询 HR"——**不编造规则**，这是企业信任的根基。

### 支柱 D：先例沉淀闭环（留痕）——人情世故的正确落地

`eval_precedent_loop.py`（四阶段闭环）：

```
S1 空白识别：出差2天变5天 → GAP → 升级 HR
S2 裁决沉淀：HR 裁决 → 案例入库（问题/裁决/理由/审批人/日期）
S3 先例复用：出差延长3天（不同措辞）→ 命中案例 → 裁决 + "仅供参考"附注
S4 边界声明：人际问题（"经理会不会卡我请假"）→ AI 明说不碰 + 制度保障建议 + 查询留痕
```

**原则**：人情世故不能规则化，但能**记录化**——AI 是"组织的记忆和镜子"，不是人际判断的替代品。难搞的人每次裁决都被留痕，隐性规则逐步显性化；员工从"战战兢兢去问"变成"先看先例、带着记录去问"。S4 的边界声明（"AI 无法也不应判断人际关系"）是**信任来源**，不是缺陷。

## 2. 数据来源（真实文档）

| 文档 | 来源 | 用途 |
|---|---|---|
| 迎新线上报到流程 | 用户提供的真实 docx（`报道.docx`，OCR 提取 11 张图 + 文字） | 流程文档域权限测试 |
| 员工管理制度通用版 | [华律网公开范本](https://www.66law.cn/contractmodel/131322.aspx)（15 条真实制度） | 制度域权限/空白/先例测试 |

内部细则为合理构造并标注"构造"；制度文本为真实来源。

## 3. 诚实边界（必须声明）

1. **检索用关键词锚定**：制度域问法模板化，关键词命中率 100%；但"措辞远离条文"的问法会 miss——生产需理解层按域建词表或接语义检索（这是明确的工程量，不是不可逾越的墙）；
2. **空白识别用词表探针**：POC 用"条款内容是否含 answer_probe"判定，生产可换 LLM 判定（成本更高但更鲁棒）；
3. **只拦实体/权限/规则级问题**：LLM 生成层若编造"不含可识别实体"的流畅假话，闸门校验不到（生成层幻觉治理是另一课题）；
4. **先例库有误导风险**：把"上次特例"当"这次规则"会出错——所有案例必须标注"仅供参考，以直属上级确认为准"；
5. **最难搞的人不会配合留痕**：若裁决拒绝记录，系统只能记录"已升级，无裁决返回"——这本身是暴露流程黑洞的有价值信号；
6. 知识库规模小（<300 对象），真实大库需 ANN + 权限预过滤（per-centroid）工程化。

## 4. 对客户的价值叙事（怎么讲）

| 客户痛点 | C-Former 回答 |
|---|---|
| "AI 答错还泄密" | 检索前权限 mask，不可见内容原理上不可达（0 泄漏三验证） |
| "AI 瞎编制度" | 空白识别 100%：明文才答，空白诚实升级，绝不编造 |
| "规则在难搞的人手里" | 先例沉淀闭环：每次裁决留痕，隐性规则逐步显性化 |
| "AI 不懂人情世故还硬答" | 边界声明：人际判断 AI 明说不碰，给制度性保障建议 |
| "出了事谁负责" | 全链路留痕：谁问的、权限判定、检索依据、升级记录、裁决案例 |

## 5. 复现

```powershell
# 支柱 A：权限闸门（检索层 + 生成层）
D:\conda\envs\cformer-gpu\python.exe rag_permission_poc.py
$env:DEEPSEEK_API_KEY = "sk-..."; D:\conda\envs\cformer-gpu\python.exe rag_permission_poc_llm.py
# 支柱 A/B：真实制度 + 复杂三级场景
D:\conda\envs\cformer-gpu\python.exe eval_employee_rules.py
D:\conda\envs\cformer-gpu\python.exe eval_employee_rules_complex.py
# 支柱 C/D：空白识别 + 先例闭环
D:\conda\envs\cformer-gpu\python.exe eval_rule_gap.py
D:\conda\envs\cformer-gpu\python.exe eval_precedent_loop.py
```

结果 JSON 存 `artifacts/`，案例库持久化在 `data/precedent_cases.json`。

## 6. 下一步（生产化路径）

1. **理解层按域适配**：企业制度域的意图词表（请假/审批/报销/考勤…）——让"措辞远离条文"的问法也能命中；
2. **LLM 判定替换探针**：空白识别/先例匹配用 LLM 提升鲁棒性；
3. **权限规则对接**：RBAC/组织架构（角色来自 HR 系统，非硬编码）；
4. **ANN 规模化**：万级文档的权限预过滤；
5. **审计面板**：谁问的/权限判定/升级记录/案例沉淀的可视化（toB 交付物）。
