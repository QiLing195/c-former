# C-Former V6.0c：区域轴修复设计

## 目标

V6.0b 盲测发现冻结 2 层编码器的两个具体弱点，本版本用**数据增强 + 重训**修复，并在同一盲测集上复验：

1. **区域轴近义别名混淆**（主要）：冰原→腹地、北部→腹地、河流下段→上段、乡村→中央。训练时查询侧与候选侧的区域词配对不足，中央/腹地/高原/冰原、城镇/田野、上段/下段在 64 维空间里互相吸引；
2. **错字稳健性弱**：单字 OOV 错字（録→绿、衬→村）2/3 种子失败。

V6.0c **不修改架构**（仍是 2 层共享 Token Transformer + 四证据门控），只改训练数据分布。修复成功后冻结为 V6.0c 编码器，作为 V6.1b 大规模索引的语义层。

## 2. 数据增强（cformer_v60c/data.py）

在 V6.0 训练数据基础上增加三类样本：

### 2.1 区域近义硬负例（region_negative）

把区域轴替换为**同一混淆簇内**的另一个区域，构造「只差一个区域」的硬负例：

```text
中央 / 腹地 / 高原 / 冰原      ← V6.0b 失败簇
北部 / 南部
东部 / 西部
海岸 / 港湾
城镇 / 田野
河流上段 / 河流下段
```

每条训练样本以 40% 概率取区域负例（随机取簇内 ≠ 当前值的区域）。

### 2.2 错字增强（typo query）

以 25% 概率把查询中一个内容字符替换为**词表外**错字（从错字池取，如 録/提/衬/滘/淸），保持目标不变。单元测试断言：错字后 coverage 下降、target 不变、且替换字不在 tokenizer 词表。

### 2.3 冲突负例（conflict_negative）

以 20% 概率取同「外观+领域+区域」、仅状态不同的对象作为硬负例（模拟双候选冲突），其余 15% 保留 V6.0 标准硬负例（mode+5）。

### 2.4 多硬负例损失

共享 `ContrastiveResolverMixin.contrastive_loss` 升级为接受**单个或多个硬负例**（`list`/`tuple` 则全部拼接进 logits），向后兼容原有单负例调用。每个训练样本带 2 个负例：区域负例 + 冲突负例。

## 3. 验证矩阵

| 验证 | 内容 | 闸门 |
|---|---|---|
| V6.0b 盲测已知 Top-1（16 条，fold=4） | 3 种子均值 | ≥ 90%（目标修复 3 个失败） |
| V6.0b 消歧（3 条） | 同名+区域+状态 | ≥ 90% |
| V6.0b 错字硬例（3 条） | 单字 OOV 错字 | ≥ 80%（V6.0 为 66.7%） |
| V6.0b 同名歧义（5 条） | 必须拒答 | ≥ 80%（期望保持 100%） |
| V6.0b 未知零误支持（6 条） | 不得 hallucinate | ≥ 95%（期望保持 100%） |
| V6.0 正式 64K 集 | 4 世界 × 96×3 查询 | Top-1 不回归 100%；已知支持率不得低于 V6.0（90.19%）5pp |
| 区域专项（新增 16 条） | 区域近义簇内两两区分 | 正确率 ≥ 90% |

所有指标报告 3 种子均值/最小/最大；失败样本全部保留。

## 4. 训练设置

- 与 V6.0 相同：2 层、d=256、8 头、FFN 768、64 维输出、AdamW(lr=1e-3, wd=1e-4)、FP16、batch 128；
- 步数提高到 **400**（数据更难，给足预算），种子 701/702/703；
- 从零重训（不继承 V6.0 检查点，避免区域权重先验污染）；
- 训练对象仍排除 holdout fold 4，盲测已知目标保持「从未见过」。

## 5. 回退条件

- 盲测已知 Top-1 未达 90% 或正式 64K Top-1 跌破 99%：本次增强方案无效，保留 V6.0 编码器并改用「区域词表扩展 + 分轴温度」方向；
- 正式集支持率大幅下降（>5pp）但 Top-1 不变：报告准确率—覆盖率权衡，不单独优化单指标。

## 6. 交付物

```text
cformer_v60c/data.py      区域/错字/冲突增强
train_evaluate_v60c.py    3 种子训练 + 三重验证
tests/test_cformer_v60c.py
artifacts/v60c_results.json
V60C_REGION_FIX_DESIGN.md / V60C_REGION_FIX_REPORT.md
```
