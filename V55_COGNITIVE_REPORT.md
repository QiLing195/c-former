# V5.5 最小认知层测试报告

## 完成内容

V5.5 在 V5.4 的版本化时空记忆上增加了可审计的 `SemanticObject`、`ObjectState`、`Transformation`、三值 `Constraint`、观察者投影和 `HypothesisSandbox`。统一世界状态只保存一份，查询时再投影到观察者坐标，避免按视角复制状态缓存。模拟结果与正式知识隔离，只有外部观测验证通过且世界版本未变化时才允许写回。

## 四个小世界

| 世界 | 主要验证内容 | 结果 |
|---|---|---:|
| 移动与坐标 | 历史/当前状态、别名、坐标投影、授权移动 | 5/5 |
| 水与冰 | Object + Transformation + Constraint、条件缺失 | 4/4 |
| 组织变化 | 延迟写入、历史角色、观察者权限 | 4/4 |
| 概念演化 | 对象生命周期、分裂、别名歧义、显式消歧 | 4/4 |

合计 17/17，错误拒绝率、不安全模拟率、未来泄漏率和身份坍缩率均为 0%。本机单次运行总耗时低于 1 ms；该数字只是小型 Python 结构化测试，不代表语言模型吞吐量。

## 消融结果

| 配置 | 准确率 | 直接风险 |
|---|---:|---|
| 完整 V5.5 | 100.00% | 四项风险率均为 0% |
| 无 Constraint | 76.47% | 不安全模拟率 100% |
| 无观察者坐标投影 | 82.35% | 坐标场景大量错误 |
| 无 ingest cutoff | 94.12% | 未来信息泄漏率 100% |
| 无身份冲突保护 | 94.12% | 分裂别名身份坍缩率 100% |

专项测试 6/6、全项目回归 33/33 通过。

## 平衡取舍

- **吸收的优点**：对象身份与生命周期、状态变化、条件化变换、世界状态与观察结果分离、假设—验证—更新闭环。
- **压制的缺点**：不允许模拟直接污染记忆；歧义身份返回冲突；条件缺失不等同于条件满足；旧世界版本的假设禁止提交。
- **没有夸大的能力**：对象别名目前显式登记，不是自动语义吸引子发现；Constraint 不是自动因果发现；Simulation 是可验证的小型状态变换，不是通用世界模型。

## 结论与下一步

V5.5 证明 Object/Transformation 思路可以作为 C-Former 外部的知识动力学骨架，而不必把所有责任塞进 Transformer。下一步应把 V5.3 的观测点神经检索器接到这个骨架前端：结构化层先执行权限、时间和版本过滤，C-Former 只在合法候选中做对象身份、Transformation 和证据重排。之后再扩大到多跳变化链和噪声文本，测试 2K/8K/32K 独立对象规模。

完整原始结果位于 `artifacts/v55_cognitive_results.json`。
