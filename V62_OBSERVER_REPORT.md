# C-Former V6.2 观测点端到端报告

日期：2026-08。前置：V6.1c 链路集成 + 真实库校准 + WorldReasoner。
目的：在真实库上验证路线图 §8 的核心假设与闸门——
"同一份知识存放在共享世界，观测点是坐标/索引而非知识副本"。

## 1. 实现

| 组件 | 文件 | 说明 |
|---|---|---|
| `ObserverFrame` / `ObserverGate` | `cformer_v62/observer.py` | 确定性可见性掩码（公司/区域两轴，None=全可见）；身份解析完成后才注入 |
| pipeline 门控挂接 | `cformer_v61c/pipeline.py` | exact / reasoned / ann 三条支持路径统一过闸；拒绝返回 `ACCESS_DENIED` 且 object_id=None（不泄漏被拒对象） |
| 治理层增量 | `cformer_v59/governance.py` | `CandidateStatus.ACCESS_DENIED` 枚举值（纯新增；门控不经 ledger 状态机） |
| 端到端评测 | `evaluate_observers.py` | 4 个观测点（中国区/美国区/开源厂商/美国开源）× 全部 known+留出查询 × 3 种子 |

## 2. 闸门结果（3 种子一致，原始 JSON 见 `artifacts/v62_observer_results.json`）

| 路线图 §8 闸门 | 目标 | 实测 | 判定 |
|---|---|---:|---|
| 身份解析在合法观测点间一致 | 一致 | **100%** | ✅ |
| 权限泄漏率 | 0 | **0** | ✅ |
| 跨视角证据召回（不同观测点×不同措辞） | ≥95% | **100%** | ✅ |
| 结论可追溯（理由非空 + 对象文档可取回） | ≥95% | **100%** | ✅ |
| 对象向量不随观测点数复制 | 单副本 | 解析前后向量数恒等 | ✅ |

## 3. 设计要点与教训

1. **先身份、后观测点**：门控只作用于已解析对象是否可见，绝不参与身份猜测；
   合法观测者之间解析结果逐位一致（含超级指代的 reasoned 路径）。
2. **掩码不是新库**：4 个观测点下向量库规模恒为 N=212——权限是坐标过滤，
   不是为每个视角复制知识副本（单副本原则断言贯穿）。
3. **评测设计教训**：跨视角召回第一版固定取两个区域互斥的观测点，
   "都有权限"的组合永不存在而得到 0%；修正为按查询动态挑选有权限观测点对。
   这提醒：闸门指标为 0 时先查测量设计，再怀疑系统。
4. 泄漏判定包含精确别名路径——即使词面直接命中被掩对象也必须拒绝
   （单元测试覆盖 exact/reasoned/ann 三条路径）。

## 4. 边界与诚实声明

1. 观测点轴目前只有公司/区域的静态权限，没有时间快照、空间方向、角色立场投影；
2. "答案生成"仍是结论级（对象+证据引用），不是自由文本解码器（路线图 V6.5 范围）;
3. 跨视角召回使用的是同模板改写体，真实口语跨视角复用需盲测集扩充；
4. 212 对象规模上的闸门通过不外推到更大规模。

## 5. 下一步

1. 时间/版本轴：ObjectState(valid_time, ingest_time) 快照查询与未来事实泄漏测试（§9 前置）；
2. 别名表接入词法锚定做指代消解（"千问"→Qwen），扩大跨视角措辞距离；
3. 数据扩量至 1K+ 后复验全部闸门的稳定性。