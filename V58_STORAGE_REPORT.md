# V5.8 开放别名候选与分层存储报告

## 实现结果

V5.8 将别名系统拆成磁盘优先的四层：SQLite B-tree 已验证别名、FTS5 内容空倒排索引、int8 单对象向量 mmap、候选别名沙箱。对象向量只保存一次，四个观测视角只保存小型 scope 记录。

## 规模与存储

| 对象数 | 等效四视角候选 | 磁盘 | 每对象 | 重复 float 视角估算 | 压缩倍数 | 懒加载内存增量 |
|---:|---:|---:|---:|---:|---:|---:|
| 32,768 | 131,072 | 27.56 MB | 882 B | 132 MB | 4.79× | 0.13 MB |
| 65,536 | 262,144 | 49.88 MB | 798 B | 264 MB | 5.29× | 0.08 MB |
| 131,072 | 524,288 | 94.25 MB | 754 B | 528 MB | 5.60× | 0.05 MB |

磁盘量随对象数近似线性增长，并没有指数爆炸；每对象字节反而随固定数据库开销被摊薄而下降。四视角没有复制对象向量，131K 对象只保存 131K 行 int8 向量，而不是 524K 行 float 向量。

## 查询性能

| 对象数 | 精确别名准确率 | 精确查询 | 开放 Top-1 | Recall@10 | 优化前开放查询 | 优化后开放查询 |
|---:|---:|---:|---:|---:|---:|---:|
| 32,768 | 100% | 0.026 ms | 100% | 100% | 211.6 ms | 4.84 ms |
| 65,536 | 100% | 0.026 ms | 100% | 100% | 409.8 ms | 6.28 ms |
| 131,072 | 100% | 0.030 ms | 100% | 100% | 836.8 ms | 9.79 ms |

最初使用所有 query term 的 `OR` 查询，公共词导致巨大 posting 集合。最终策略先对带类别/属性编码的高信息词做 `AND` 交集，再对最多 64 个候选读取 mmap int8 行并评分。查询内存增量约 8 MB，与完整世界规模基本无关。

## 构建性能

32K/64K/128K 的构建时间分别约 42/84/168 秒，稳定在每秒约 780 个对象。当前瓶颈是 CPU 上逐对象文本哈希和 SQLite 写入，不是加载或神经计算。

## 开放别名安全

- 未登记表述只生成 `alias_candidates(status='proposed')`；
- 候选不会自动进入正式 alias 表，因此已提交错误合并率为 0；
- 外部审核后才能变为 `verified`；
- alias 撤回带世界版本，历史快照仍可查询；
- 一个已验证别名指向多个活跃对象时返回 `ambiguous`。

## 限制

- 开放表达测试使用可组合的合成属性词，100% 结果不代表真实同义句理解；
- 高信息词 `AND` 适合结构化属性，完全自由文本仍需要 ANN/语义编码回退；
- SQLite FTS 和向量文件当前是单机索引，没有测试并发写入与故障恢复；
- CUDA 尚未使用：本机 GPU 可用，但当前项目 PyTorch 是 CPU 版，需要独立 CUDA 环境。

## 下一步

1. 增加无共享属性词的真实释义测试；
2. 建立 `verified exact -> sparse shard -> semantic ANN -> Top-64` 三级回退；
3. 创建独立 CUDA PyTorch 环境，用 GPU 只训练/编码/重排，数据库继续留在 CPU；
4. 扩展到 256K/512K 对象并测试多进程读取；
5. 以错误合并率而非只看 Recall 作为开放对象发现的首要门槛。

原始优化结果位于 `artifacts/v58_storage_results_optimized.json`。
