# C-Former V6.3：受控递归层设计（承接关系推理）

## 动机：真实数据实验划出的任务边界

三域真实数据实验（V62 报告）证明：

- 身份解析层（name/alias）在真实数据上 **76.3%**（分域评测）；
- **关系推理（predecessor/latest）只有 33%**——「GPT 系列最新是哪个」「前一代是 GPT-4o 的是哪个」这类**系列内比较 / 前代链推理**，不是检索式身份解析的职责；
- 观测点层已验证（selection 92.2% / invariance 97.2% / permission 100%）。

V6.3 的目标：**把 predecessor/latest 这类关系推理从身份层剥离，交给专用的受控递归层**，并用真实数据（AI 模型系列链）作为首个测试域。

## 1. 设计原则（对齐路线图 §9）

1. **递归 = 共享参数小模块重复多次**（2 个共享块 × 1–4 跳），不线性堆层；
2. **确定性控制器优先于神经网络**：循环、深度、时间单调、版本固定全部由确定性逻辑兜底；
3. **每跳可重放**：答案必须能回溯完整推理路径（对象→关系→证据），不能只给最终向量；
4. **身份先于递归**：递归层只处理「已确认身份的对象集合」上的关系查询，不重新做身份猜测。

## 2. 数据结构（真实数据映射）

| 概念 | 真实数据示例（AI 模型域） |
|---|---|
| `ObjectIdentity` | GPT-5.2（稳定身份） |
| `ObjectState(object, version, time)` | GPT-5.2 于 2026 年发布（v 版本） |
| `Transformation(source, target, effect)` | GPT-5.1 → GPT-5.2（前代关系） |
| `RelationChain(series)` | GPT-1 → GPT-2 → … → GPT-5.2（系列链） |
| 查询快照 | world_version + 时间截止点固定 |

国家域/电影域同样适用：国家没有"前代"，但可以有"隶属/邻国"链；电影有系列链（漫威宇宙、哈利波特）。

## 3. 递归机制

```text
查询「GPT系列最新的是哪个？」
  → 身份层确认候选集合 = {GPT-1..GPT-5.2}（已确认对象）
  → 递归块沿 series 链传播：
      hop1: GPT-5.2 的变化/关系证据（"是该系列最新版本"）
      hop2: 检查是否存在"后继"（无后继 ⇒ 最新）
  → 确定性终止：深度 ≤4、无循环、时间单调、版本固定
  → 输出：GPT-5.2 + 完整路径 [GPT-5.1 → GPT-5.2] + 证据 ID
```

## 4. 与身份层/观测点的接口

```text
身份解析层（V6.0 冻结）──候选对象 + 身份
        │
观测点层（确定性 mask）──可见对象集
        │
V6.3 递归层 ──关系查询 → 路径推理 → 证据答案
        │
   EvidenceVerifier（分类型阈值）
```

## 5. 测试矩阵（真实数据驱动）

| 项 | 内容 | 闸门 |
|---|---|---|
| 1–4 跳任务 | AI 系列链：GPT/Claude/Gemini/Qwen/DeepSeek 各系列的前代链、最新查询 | 成功率 ≥90% |
| 环路 | 人为构造循环关系 | 100% 终止 |
| 深度 | >4 跳任务 | 100% 拒绝并报告 |
| 时间 | 查询时间早于对象发布时间 | 100% 拒绝（未来泄漏 0） |
| 版本 | 递归期间世界版本改变 | 返回 version_changed，不混合快照 |
| 参数 | 共享递归 vs 直接堆 4/8 层 | 相近准确率下省 ≥30% 推理层参数 |
| 对照 | V6.0 身份层直接答（33%） | 递归层应显著高于此 |

## 6. 首个测试域

AI 模型域（273 对象）天然有丰富的系列链与版本关系，是 V6.3 的第一个测试域：
- 36 个系列的前代链（如 GPT-1→…→GPT-5.2）；
- `predecessor` / `latest` 查询（当前身份层 33%，目标递归层 ≥90%）；
- 国家/电影域作为第二、三测试域（验证递归机制域无关）。

## 7. 交付物

```text
cformer_v63/         递归块 + 确定性控制器（循环/深度/时间/版本）
build_recursive_queries.py  从多域数据集生成 1–4 跳递归查询
tests/test_cformer_v63.py
train_eval_v63.py
V63_RECURSION_REPORT.md
```

## 8. 回退条件

- 递归成功率不高于身份层直接答（33%）→ 机制无效，保留身份层现状；
- 共享递归相对堆层参数节省 <30% → 采用直接堆层；
- 任何循环/版本/时间泄漏 → 该轮结果作废，确定性控制器优先修复。
