# V5.7 噪声文本与多跳 Transformation 设计

## 目标

V5.7 把 V5.5/V5.6 的规范对象和神经重排接到真实字符串入口，验证四类文本：规范名称、已登记但训练未固定记忆的别名、Unicode/空格/标点噪声、长上下文干扰。同时增加受控多跳 Transformation 路由。

## 处理链

```text
原始文本
  -> Unicode NFKC / 大小写 / 空格归一化
  -> 单字、词和 2/3 字符 n-gram 哈希特征 + 8维规范对象键
  -> 文本对象 Recall@64
  -> C-Former 观测点重排
  -> V5.5 权限/时间/版本/Constraint
  -> 受控多跳 Transformation
```

字符哈希层是轻量、可审计的词法适配器，不宣称理解开放语义。8维对象键来自 V5.5 已解析的规范身份，防止大规模相似文本的哈希碰撞；别名只有与规范文档共享编号或已写入对象记录时才能可靠匹配。

## 多跳边界

路由器在每一跳检查事件时间、写入时间、观察者权限和世界版本；重复对象终止为 `cycle`，超过预算终止为 `depth_limit`，版本变化终止为 `version_changed`。神经/词法选择器不能绕过这些终止条件。

## 对照

- 观测点门控 C-Former；
- 等参数加法 Evidence-RAG；
- 关闭观测点；
- 纯词法对象 Top-1；
- 文本 Recall@64 与神经重排准确率分开统计。
