# C-Former V6.1：ANN 分层检索设计

## 目标与边界

V6.0b 之后冻结了 2 层编码接口（64 维归一化对象向量）。V6.1 的目标是把在线检索从 `O(N)` 全量扫描降为**近似次线性**，同时不损失最终解析质量：

> ANN 粗召回 Top-256（Recall≥99.5%）→ 四证据 C-Former 精确精排 Top-16 → score/margin/coverage 边界判定。

本版本在 V6.0 对象空间（最多 65,536 个对象）上实现并验证第一版 ANN 索引；512K+ 的规模测试按路线图留到 V6.1b（faiss HNSW/IVF-PQ 与 V5.9 磁盘层对接）。

### 诚实的边界

- 本版本用**纯 torch 的 IVF（倒排文件）**实现：零新依赖、GPU 友好、可在 CI 复现；它不是 HNSW，不宣称达到工业级 1M+ 延迟；
- 向量来自冻结 V6.0 L2 编码器（不重新训练）；ANN 只改检索结构，不改语义；
- 「精确别名 B-tree → FTS/属性分桶」两级仍在 V6.1b 与 V5.8 磁盘层对接，本版本聚焦「向量粗召回 + 精排」这一环。

## 2. 检索链路（V6.1 目标形态）

```text
查询文本 → 冻结 Token Transformer 编码 → q (64 维)
                │
        IVF 倒排索引（本版本实现）
        │  nprobe 簇 + 余弦粗召回
        ▼
        候选 Top-256
                │
        精确余弦精排（候选内，无全量扫描）
        ▼
        score/margin/coverage verifier
        → supported / ambiguous / unknown
```

IVF 训练：在对象向量子样本上做 k-means（torch，余弦分配），把每个对象归入最近簇；查询时只探测 nprobe 个簇，在簇内精确打分取 Top-256。

## 3. 数据格式与存储

| 项 | 设计 |
|---|---|
| 向量 | FP16（每对象 128 B）与 INT8（每对象 64 B + 每向量 FP16 scale）双格式 |
| 索引 | 簇质心（FP32）+ 每簇对象 id 列表 + 向量张量；版本号绑定 |
| 追加 | `add(vectors, ids)` 追加到向量库并归簇 |
| 删除 | `remove(ids)` 墓碑标记，搜索排除，不物理删除 |
| 快照/回滚 | `snapshot(path)` 保存版本化状态；`restore(path)` 回滚；失败可回退到上一可用快照 |
| 单副本 | 一个对象一条向量，不随别名/问题/观测点复制 |

## 4. 测试矩阵

| 项 | 本版本 | 闸门 |
|---|---|---|
| 规模 | 16K / 32K / 64K（V6.0 最大对象空间） | — |
| 世界 | 每规模 4 个固定世界（seed 60/161/262/363） | — |
| 查询 | 每世界 96 条 heldout 已知 + 16 条 V6.0b 盲测已知 | — |
| 召回 | IVF Recall@256（nprobe 8/16/32/64 扫描） | ≥ 99.5% |
| 最终质量 | ANN+精排 Top-1 vs 精确全量 Top-1 之差 | ≤ 0.5 个百分点 |
| 存储 | FP16 ≤ 8 MiB @64K；INT8 ≤ 4 MiB @64K | 通过 |
| 延迟 | 查询 p50/p95（GPU） | p95 < 10 ms @64K（路线图 1M 目标的本版本参照） |
| 一致性 | 小库上 ANN Top-k 与暴力全量一致；墓碑/快照/回滚单元测试 | 100% |

## 5. 质量闸门与回退

1. nprobe 足够时 Recall@256 ≥ 99.5%，否则**不得**上线 ANN；
2. ANN+精排相对精确扫描 Top-1 下降 ≤ 0.5 个百分点，否则回退「精确扫描」路径；
3. 增量写入失败后必须能回到上一可用快照，正式对象不被候选自动污染；
4. 回退条件（路线图 §7 原文）：ANN 召回不达标时保留「精确别名 + FTS shard + shard 内精确向量」混合路径，不以速度换取不可控漏召回。

## 6. 交付物

```text
cformer_v61/           IVF 索引 + FP16/INT8 向量库
tests/test_cformer_v61.py
evaluate_v61.py        冻结编码器 + 4 世界 × 3 规模评测
artifacts/v61_results.json
V61_ANN_DESIGN.md / V61_ANN_REPORT.md
```

## 7. V6.1b（后续）升级路径

- faiss HNSW（延迟）与 IVF-PQ（内存）对照，目标 256K/512K/1M；
- 与 V5.8 SQLite/FTS/mmap 磁盘层对接：精确别名命中即返回，未命中才走 ANN；
- 索引与对象版本绑定、增量建索引、快照切换（对应路线图 V6.1 工程内容）。
