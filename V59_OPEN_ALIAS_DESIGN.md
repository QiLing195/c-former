# V5.9：无编号开放别名与多证据对象解析

## 目标

V5.9 解决 V5.7/V5.8 中最重要的有效性缺口：查询别名不再包含对象编号、可解析代码或已知对象键。模型只能根据语义描述，在共享对象世界中找到对象；对象身份确定后，观测点才参与后续的权限、视角投影和推理。

本版不是用神经网络替代 SQLite、版本控制或 C-Former 推理层，而是在它们前面增加一个受控的开放表达解析层。

## 架构

```text
无编号查询文本
    │
    ├─ WordTokenizer ─ Query Encoder ───────────────┐
    │                                               │ cosine / streaming Top-K
共享对象库                                           ├─────────────────────────┐
    ├─ 名称证据 ─┐                                  │                         │
    ├─ 属性证据 ─┼─ Evidence Encoder + learned gate ┘                         │
    ├─ 关系证据 ─┤                                                            ▼
    └─ 变化证据 ─┘                                                  EvidenceVerifier
                                                                       │
                                      ┌───────────────┬────────────────┼──────────────┐
                                      ▼               ▼                ▼              ▼
                                  supported       ambiguous         unknown        rejected
                                      │
                                      ▼
                              CandidateLedger（候选态）
                                      │ 显式人工/外部审核
                                      ▼
                                  verified ──冲突──> rolled_back
                                      │
                                      ▼
                         V5.8 正式别名索引 / 共享对象身份
                                      │
                                      ▼
                         观测点过滤 + C-Former 后续推理
```

### 关键职责边界

1. **身份解析与观测点分离**：同一表达在不同观测点下应先解析到同一对象；观测点只控制可见事实、关系方向和回答方式，不能改变对象身份。
2. **四证据融合**：对象向量不是重复保存四个视角，而是由名称、属性、关系、变化四类文本经共享编码器和门控融合得到。
3. **模型不能自动验证**：模型最高只能产生 `supported`。只有 `reviewer:*` 身份可以把候选转为 `verified`。
4. **可回滚**：候选状态和每次迁移都有版本与原因；已验证别名发现冲突后进入 `rolled_back`，不静默覆盖历史。

## 无身份泄漏数据

合成世界由四个语义轴组合：

| 语义轴 | 取值数 | 示例（对象文本 → 查询别名） |
|---|---:|---|
| 颜色/外观 | 32 | `red → crimson` |
| 领域/用途 | 32 | `navigation → routing` |
| 区域/关系 | 35 | `north → arctic` |
| 模式/变化 | 16 | `stable → steady` |

组合空间共有 573,440 个对象。对象文本使用左侧词，查询使用右侧同义表达；任何渲染文本都不包含数字、对象 ID 或身份键。对象 label 只作为评测答案存在，不进入 tokenizer 或模型。

训练与测试按语义组合的 family fold 隔离。模型训练时见过单个语义词，但没有见过测试对象的完整组合，因此测试的是组合泛化，而不是对象编号记忆。

## 训练内容

- 300 步，batch size 256，共 76,800 个正查询—对象对。
- 每个正例带一个只改变“模式”字段的硬负例，迫使模型处理近边界对象。
- 使用 in-batch contrastive loss；查询编码器与候选四证据编码器输出 64 维归一化向量。
- 对照模型是相同步数、相同词表、相同 64 维输出的两层 Transformer dual encoder。

## 边界策略

`EvidenceVerifier` 同时检查查询词表覆盖率、第一候选绝对相似度、第一与第二候选的 margin。查询省略一个能区分真实库内对象的字段时，测试集保证库中确有冲突对。低 margin 返回 `ambiguous`；大面积 OOV 返回 `unknown`；完整证据且 margin 足够时只返回 `supported`，仍需审核。

## 存储与复杂度

当前性能基准把候选向量保存为 FP16，64 维对象向量占 `128 × N` 字节：512,000 对象为 62.5 MiB。这是一份对象级向量，不随观测点数量复制。

当前检索使用分块精确点积，空间复杂度为 `O(Nd)`，单查询计算复杂度仍是 `O(Nd)`。它适合验证正确性，但不是最终生产检索方案。下一版应把 V5.9 编码器接到 V5.8 磁盘层，并增加 ANN 粗召回，形成“ANN Top-K → 多证据精排 → 边界验证”的三级路径。

