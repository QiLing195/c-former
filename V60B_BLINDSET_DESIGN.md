# V6.0b 真实中文盲测集设计

## 目标与边界

V6.0 正式测试的 55,296 次查询全部由 6 条模板生成，Top-1 在 64K 上达到 100%——这不能证明冻结的 2 层编码器能理解**模板之外的自然中文表达**。V6.0b 建立第一个与训练模板隔离的盲测集，回答一个具体问题：

> 冻结的 V6.0 L2 编码器，在自然句式、口语化表达、错别字、同名不同对象、自然未知表达和冲突证据下，还能保持 100% 的解析能力吗？

本版本**不修改模型、不重新训练**，只新增数据与评测，报告失败案例。

### 诚实的边界（必须声明）

- 盲测文本是**人工撰写**的自然中文，但受冻结 tokenizer 词表约束（见 §4），不是互联网真实语料；它证明「模板外句式」的泛化，不证明「任意开放世界实体发现」；
- 对象仍来自 4 语义轴组合空间（与 V6.0 相同），保证冻结检查点可以直接评测；
- 真正的开放语料（V6.0c+）需要人工标注或真实知识来源，超出本版本范围。

## 2. 盲测集结构

| 类别 | 数量 | 构造方式 | 预期状态 |
|---|---|---|---|
| 模板外已知查询 | 12 | 12 条新写的自然句式 × 12 个 holdout(fold=4) 对象 | supported + Top-1 正确 |
| 人工定制已知查询 | 4 | 逐条手写，目标值折 fold=4 | supported + Top-1 正确 |
| 错别字/口语硬例 | 3 | 在定制查询上替换 1 个字符为生僻/错字 | 报告 Top-1（不设闸门） |
| 同名不同对象 | 5 | 共享「外观+领域」、区域/状态不同的对象对 | ambiguous |
| 同名 + 区域消歧 | 3 | 与同名对同一对象，查询补充区域 | supported + Top-1 正确 |
| 自然未知表达 | 6 | 词表外新实体、OOV 口语、缺失信息 | unknown |
| 冲突证据 | 3 | 同一对象两种状态断言、双候选引用 | 报告（不设闸门） |

合计 36 条。全部文本满足：

- 不含数字、对象编号、`label`/`object_id`（`assert_identity_free`）；
- 不含 V6.0 六条模板的特征短语（§3 的禁止片段清单）；
- 已知/同名/消歧/冲突查询的 tokenizer coverage ≥ 0.6，保证 verifier 不会因 coverage 直接拒答；
- 未知查询刻意携带 OOV 字符或缺失信息，验证「低 coverage / 低分数」两种拒答路径。

## 3. 模板独立性约束

V6.0 六条模板去除占位符后的特征短语，盲测文本一律不得包含：

```text
请寻找位于 / 我需要一个外观 / 目标用途是 / 忽略背景中的其他装置 / 排除 / 不要
```

单元测试逐条断言。此清单保证盲测查询不可能由 V6.0 数据生成器产出。

## 4. 冻结 tokenizer 约束

V6.0 的字符 tokenizer 词表来自：4 轴规范词 + 4 轴别名词 + 模板固定文字 + 5 条证据句。V6.0b 无法扩展词表（扩展会改变 embedding 索引，破坏冻结检查点兼容）。因此盲测文本：

- 使用别名词（赤红、路径指引、河流上段、持续转动…）与模板功能字（的/在/是/目前/状态/装置…）；
- 允许少量 OOV 口语字（帮/看/下/吗/什/么/哪…），但已知类查询保证 coverage ≥ 0.6；
- 未知类查询利用 OOV 密集文本制造自然拒答，而非沿用 V6.0 固定字符串 `qxz nebula unsupported identity`。

## 5. 评测协议

- 设备：`cformer-gpu`（RTX 3050），与 V6.0 正式环境一致；
- 检查点：`artifacts/v60_strict_checkpoints/cformer_L2_seed601/602/603.pt`（3 种子均值/最小/最大）；
- 对象库：`ChineseAliasWorld(65536, seed=60)` 全量 64K 候选，FP16 单副本；
- 边界器：`EvidenceVerifier(minimum_score=0.50, minimum_margin=0.08, minimum_coverage=0.60)`——与 V6.0 完全相同的固定阈值，不做任何调参；
- 校准扫描：margin ∈ {0.02…0.20} 输出 coverage 与 supported 风险曲线，为 V6.0b 报告第 2 步（独立验证集校准）提供第一组数据；
- 输出：`artifacts/v60b_results.json`（逐查询记录 + 错误样本 + 校准曲线）。

## 6. 质量闸门

| 指标 | 目标 | 说明 |
|---|---|---|
| 已知 Top-1（模板外 12 + 定制 4） | ≥ 90%（3 种子均值） | 模板外句式的主要闸门 |
| 同名歧义检出率（5 条） | ≥ 80% | 同名不同对象必须拒答而不是乱猜 |
| 自然未知拒答率（6 条） | ≥ 95% | OOV/缺信息必须拒答 |
| 泄漏检查 | 100% | 无数字/编号/模板片段 |
| 消歧正确率（3 条） | ≥ 90% | 同名 + 区域必须消歧成功 |

未达标即为**发现**：把失败查询逐条写入报告，作为 V6.0c 校准或 V6.1 接口调整的输入，而不是偷偷调阈值掩盖。

## 7. 交付物

```text
cformer_v60b/           盲测数据包
tests/test_cformer_v60b.py
evaluate_v60b.py        冻结检查点评测 + 校准扫描
artifacts/v60b_results.json
V60B_BLINDSET_REPORT.md
```
