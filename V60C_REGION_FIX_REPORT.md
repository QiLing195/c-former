# C-Former V6.0c 区域轴修复报告（负结果）

## 结论

V6.0c 试图用**数据增强 + 重训**修复 V6.0b 发现的区域轴近义别名混淆。两轮迭代后结论是**负结果**：

> 简单数据增强**不能**在保持正式集 100% 的同时修复区域轴；它用正式集的精确度换取了标准句式上的区域稳健性，且对盲测自然句式的已知 Top-1 没有提升。

**保留 V6.0 编码器作为冻结基线**（`v6.0` 标签不变）。V6.0c 的代码、测试、检查点作为可复现的失败实验保留，但**不晋升为冻结版本**。

## 两轮迭代

| 轮次 | 增强配方 | 步数 | 结果 |
|---|---|---|---|
| 迭代 1 | 区域负例 + 冲突负例(mode+1) + 全字符错字(25%) | 400 | 正式 Top-1 99.2%、错字 33%、消歧 100%——冲突负例与功能字错字太硬 |
| 迭代 2 | 区域负例 + 标准负例 + 内容字错字(15%) | 500 | 正式 Top-1 99.6%、错字 55.6%、消歧 66.7%——仍回退 |

## 迭代 2 结果（3 种子 701/702/703）

| 指标 | V6.0 基线 | V6.0c 均值 | 最小 | 最大 | 判定 |
|---|---:|---:|---:|---:|---|
| 盲测已知 Top-1（16 条） | 93.75% | 93.75% | 87.5% | 100% | ❌ 无提升 |
| 盲测消歧（3 条） | 88.9% | 66.7% | 66.7% | 66.7% | ❌ 回退 |
| 盲测错字（3 条） | 66.7% | 55.6% | 33.3% | 66.7% | ❌ 回退 |
| 盲测同名歧义（5 条） | 100% | 100% | 100% | 100% | ✅ |
| 盲测未知零误支持（6 条） | 100% | 100% | 100% | 100% | ✅ |
| **正式 64K Top-1** | **100%** | **99.57%** | 99.48% | 99.74% | ❌ 回退 |
| 正式已知支持率 | 90.19% | 85.24% | 82.55% | 87.5% | ❌ 回退 |
| 正式歧义检出 | 99.91% | 99.57% | 98.70% | 100% | ≈持平 |
| 正式未知拒绝 | 100% | 100% | 100% | 100% | ✅ |
| 区域专项（16 条） | — | 100% | 100% | 100% | ✅ 增强有效 |

## 分析：为什么失败

1. **区域专项 100%、盲测自然句式没提升**——区域增强只让模型在**标准句式**下更好地分开「冰原/腹地/上段/下段」，但盲测失败发生在**人工自然句式**（口语词序、错字、`是不是` 反问等）。增强没有触碰真正的困难点：自然句式与区域词的**组合泛化**。

2. **正式集回退是必然的权衡**——正式集在 64K 上饱和 100%，任何把决策边界推向「更细的区域区分」的扰动都会挤占原先贴合的 margin 空间，表现为已知支持率从 90.19% 降到 85.24%（更多查询被判 ambiguous/unknown 而非错误，Top-1 从 100% 掉到 99.57% 是少数查询 margin 跌破阈值）。

3. **指向校准而非权重**——回看 V6.0b 校准曲线：冻结 margin=0.08 时盲测文本的 coverage 只有 28.8%、supported 风险 5.3%。盲测的「失败」大量是 **margin 过小被判 ambiguous**，而不是「高分错答案」。这说明问题在**阈值/校准层**（按查询类型分校准 margin），而不是编码器权重。重训权重是错的方向。

## 闸门判定（对照设计 §3/§5）

- 盲测已知 Top-1 未提升（≤ V6.0 的 93.75%）；正式 64K Top-1 跌破 99.9%（99.57%）；
- 触发设计 §5 回退条件：**「本次增强方案无效，保留 V6.0 编码器」**。

## 下一步建议（按优先级）

1. **阈值校准路径（首选）**：在独立验证集上按查询类型（完整/缺字段/未知/区域近义/错字）分校准 margin，输出风险—覆盖率曲线与误接受率；不碰权重；
2. **区域轴归纳偏置**（若校准后仍不够）：给区域轴单独的嵌入/位置结构，或把区域从「四证据平均门控」改为显式结构化特征——这属于 V6.1 接口设计，不是 V6.0c 的数据增强；
3. **接受现状推进**：V6.0 + 校准阈值作为 V6.1b 的语义层，把盲测 93.75% 作为已知边界写进 V6.1b 报告，不掩盖。

## 复现

```powershell
D:\conda\envs\cformer-gpu\python.exe -m pytest tests/test_cformer_v60c.py
D:\conda\envs\cformer-gpu\python.exe train_evaluate_v60c.py --seeds 701 702 703 --steps 500 --batch-size 128
```

逐种子指标见 `artifacts/v60c_results.json`（per_seed）。检查点 `artifacts/v60c_checkpoints/` 保留供审计，不作为冻结基线。
