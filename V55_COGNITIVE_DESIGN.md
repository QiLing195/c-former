# V5.5 最小认知层设计

## 目标

V5.5 吸收 Semantic Object / Knowledge Dynamics 中可验证的部分，但不把尚未解决的“语义吸引子发现、自动因果发现、自由世界模拟”冒充成已有能力。它在 V5.4 的时间、空间、权限和版本框架上增加四个部件：

1. `SemanticObject`：显式身份、别名、有效期、生命周期和谱系；
2. `ObjectState`：对象在事件时间和版本下的可审计状态；
3. `Transformation`：带约束和证据的状态变换；
4. `HypothesisSandbox`：模拟与正式记忆隔离，验证后才允许提交。

## 架构

```text
问题 + 观测点 + 查询时间
          |
          v
对象身份解析 ---- 歧义 -> CONFLICT
          |
          v
版本/写入时间/权限过滤
          |
          v
统一世界状态 --坐标变换--> 观测点状态
          |
          v
Transformation + Constraint(满足/不满足/缺失)
          |
          v
假设沙箱 --外部观测验证--> 版本化写回
```

世界状态与观测结果分离。同一对象只保存规范状态，不为每个观测点复制缓存；观测时再转换到观察者坐标。Transformation 的输出只是候选假设，不能绕过验证、权限或世界版本检查。

## 有意保留的限制

- 对象身份和别名目前是显式登记，不宣称已经学会跨模态语义吸引子；
- Constraint 是显式条件，不宣称仅靠观察数据完成了因果发现；
- Simulation 只是确定性小型状态变换，不是通用世界模型；
- C-Former 神经检索仍应在后续作为候选关联/重排层接入，不能替代安全控制器。

## 验证世界

- 移动与坐标：同一对象的历史状态、当前状态、不同观察坐标和受权移动；
- 水与冰：`Object + Transformation + Constraint -> New Object`；
- 组织变化：延迟写入、历史角色和观察者权限；
- 概念演化：别名、分裂、生命周期以及歧义拒答。
