# V5.6 结构化认知记忆与神经 C-Former 联合设计

## 责任边界

```text
对象名/问题/观测点/时间
          |
          v
V5.5 确定性控制器
身份、权限、事件/写入时间、版本、冲突
          |
          v
对象分片 + Top-64 合法候选
          |
          v
C-Former 观测点门控重排
          |
          v
选中的 ObjectState / Transformation
          |
          v
V5.5 Constraint + HypothesisSandbox
```

神经模型没有权限恢复被过滤的候选，也不能直接写记忆。对象身份已经由结构化层解析，因此候选索引先取该对象的四个视角，再加入硬负例；无论世界为 2K、8K 还是 32K，神经层严格只处理 64 个候选。

## 公平对照

- C-Former：问题表示与观测点表示通过可学习门控交互；
- Evidence-RAG：观测点作为普通加法特征；
- No Observer：关闭观测点输入；
- Rough Top-1：不做神经重排；
- Full Scan：神经表示扫描全部候选，仅测延迟和缓存代价。

C-Former 和 Evidence-RAG 参数完全相同、初始化相同、训练批次相同。目标视角由对象状态、问题类型和观测点共同决定，从而避免模型只记住固定视角规则，并测试加法条件与门控交互的差异。

## 诚实边界

这是合成的对象/Transformation 检索实验，不是自然语言生成实验。结构化索引已经知道规范对象身份，神经层验证的是视角条件化重排，而不是开放世界实体发现。后续只有在规模稳定、对照显著后，才进入噪声文本和自动对象对齐。
