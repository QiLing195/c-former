# C-Former V3：分层可扩展记忆

## 目标

在不删除原始信息的前提下，避免每个问题都让全部事实进入高成本 Transformer：

```text
原始事实（完整保留）
    -> 低维全局索引
    -> Observer + Question 两阶段 Top-K
    -> 仅对候选事实做精确 Cross-Attention
```

## 两阶段查询

第一阶段从问题和观测点召回第一跳证据；查询状态吸收证据后，再检索第二跳证据。当前结构化事实任务每阶段取 Top-1，第二轮精确推理同时看到两轮候选。Top-K 扫描显示，扩大候选反而会引入同类型事实干扰；自然语言歧义任务应重新校准 K。

训练包含答案分类损失和两阶段证据检索损失。Top-K 之外的原始事实仍保存在低层存储中，可在下一轮检索中访问。

## 规模梯度

- 128 条事实
- 512 条事实
- 2,048 条事实

每个规模使用 5 个固定世界、每个世界 36 个问题。模型训练只使用 128 和 512 规模，2,048 用于规模外推测试。

## 对照

- `DenseConcatTransformer`：每个问题都把全部事实、观测点、问题拼接并重新编码。
- `HierarchicalCFormer`：世界编码一次并缓存，通过低维索引和 Top-K 精确推理回答多个问题。
