# C-Former V6.1b：IVF 512K 规模报告

## 结论

IVF 倒排索引在**真实编码器向量**上从 128K 一路扩展到 512K，全程 **Recall@256 = 100%、ANN+精排相对精确扫描 Top-1 下降 = 0**，且内存线性、建索引亚秒级、查询 p50≈1.5 ms：

- 512K FP16 对象向量库 **64.0 MiB**（正好命中路线图「512K FP16 ≤64 MiB」闸门）；
- 512K INT8 **33.0 MiB**（比路线图「≤32 MiB」目标超 1 MiB，见 §4）；
- 512K IVF 建索引 **0.33 s**（向量化 k-means），查询 p50 1.5 ms / p95 2.2 ms；
- 墓碑删除、快照回滚、单副本存储沿用 V6.1 单元测试（6 项）。

**但必须诚实**：这是 V5.9 组合语义世界（英文词表），向量仍然高度结构化、无真实噪声，Recall 100% 依然是「太容易」。真正的 ANN 压力测试需要**非组合、含近重复与噪声的真实对象向量**，本报告不据此宣称 ANN 在开放世界同样 100%。

## 测试方法

- 编码器：V5.9 `SemanticDualEncoder`（64 维，重新训练 300 步，37,185 参数）；
- 世界：`OpenAliasWorld`（最大 ~92.9 万对象），规模 131,072 / 262,144 / 524,288，seed 59；
- 查询：每规模 128 条 heldout(fold=4) 已知查询；
- IVF：簇数 = max(1024, scale/64)，nprobe ∈ {16, 32, 64, 128}，粗召回 Top-256 → 候选内精确精排 Top-1；
- 对照：同查询的批量精确扫描 Top-1。

## 结果（nprobe=16 已达标，全 nprobe 一致）

| 规模 | 簇数 | Recall@256 | ANN Top-1 | 精确 Top-1 | Top-1 下降 | p50 ms | p95 ms |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 131,072 | 2,048 | 100% | 100% | 100% | 0 | 1.5 | 2.2 |
| 262,144 | 4,096 | 100% | 100% | 100% | 0 | 1.7 | 4.1 |
| 524,288 | 8,192 | 100% | 100% | 100% | 0 | 1.5 | 2.2 |

nprobe 16/32/64/128 的 Recall@256 全部为 1.0。相对精确扫描，ANN 在全部规模、全部 nprobe 下 Top-1 零损失。

## 存储（线性增长）

| 规模 | FP16 | INT8 | FP16/对象 | INT8/对象 |
|---:|---:|---:|---:|---:|
| 131,072 | 16.0 MiB | 8.25 MiB | 128 B | 66 B |
| 262,144 | 32.0 MiB | 16.5 MiB | 128 B | 66 B |
| 524,288 | 64.0 MiB | 33.0 MiB | 128 B | 66 B |

INT8 每对象 66 B = int8 主体 64 B + FP16 每向量 scale 2 B。512K INT8 = 33.0 MiB，比路线图「≤32 MiB」超 1 MiB；去掉 scale 列（向量已归一化，固定 scale=1/127 量化）可省 2 B/向量回到 32 MiB，V6.1c 实施。

## 工程修正（本版本内）

1. **向量化 k-means**：`train()` 用 `index_add_` 聚合簇内求和，替代逐簇 Python 循环。512K、8192 簇建索引从「预计数分钟」降到 0.33 s；
2. **分块簇分配**：`add()` 的全量 assignment 矩阵乘（512K × 8192 = 17 GB）在 4 GB GPU 上 OOM，改为 8192 行分块，峰值内存回到 ~134 MB；
3. 两处改动均通过原 6 项 IVF 单元测试，无行为变化。

## 质量闸门（路线图 §7）

| 闸门 | 目标 | 结果 |
|---|---|---|
| ANN Recall@256 | ≥ 99.5% | ✅ 100%（128K–512K） |
| ANN Top-1 相对精确下降 | ≤ 0.5pp | ✅ 0.00pp |
| 512K FP16 ≤ 64 MiB | ✅ | ✅ 64.0 MiB |
| 512K INT8 ≤ 32 MiB | ❌ | ❌ 33.0 MiB（超 1 MiB，见 §4） |
| 1M GPU p95 < 10 ms（512K 参照） | — | ✅ 2.2 ms |

## 剩余边界（V6.1c）

- 真实非组合对象向量（近重复、噪声、长尾）下的 Recall 压力测试——这是 ANN 是否真正有意义的判据，需要真实语料；
- INT8 固定 scale 量化省下 2 B/向量，满足 32 MiB 闸门；
- 与 V5.8 磁盘层串联（精确别名 B-tree → FTS → ANN → 精排）的端到端链路；
- faiss HNSW 对照（延迟）与并发/故障注入。

## 复现

```powershell
D:\conda\envs\cformer-gpu\python.exe -m pytest tests/test_cformer_v61.py
D:\conda\envs\cformer-gpu\python.exe bench_v61_scale.py --scales 131072 262144 524288
```

原始逐行结果见 `artifacts/v61_scale_results.json`。
