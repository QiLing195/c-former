# C-Former V6.1 ANN 分层检索报告

## 结论

V6.1 用**纯 torch 的 IVF 倒排索引**把「全量精确扫描」替换为「粗召回 + 精排」，在 V6.0 冻结 2 层编码器的全部规模上达到：

- **Recall@256 = 100%**（3 规模 × 4 世界 × nprobe 16/32/64/128 全矩阵）；
- **ANN+精排相对精确扫描 Top-1 下降 = 0**（16K/32K/64K 全部为 0，盲测集 64K 同样为 0）；
- 64K FP16 向量库 **8.0 MiB**（=8,388,608 B，符合预算），INT8 **4.1 MiB**（65,536×66 B）；
- 64K、nprobe=16 下查询 **p50 ≈ 2.2 ms、p95 ≈ 3.2 ms**（向量化搜索路径，见 §6），IVF 建索引 7.5 s、增量写入 0.02 s；
- 墓碑删除、快照/回滚、单副本存储全部通过单元测试（6 项）。

在 V6.0 的结构化对象空间里，IVF 甚至 nprobe=16（只探测 1.6% 的簇）就能零漏召回，说明该空间的向量聚类质量极高；但这也意味着**本结果不能外推到真实开放世界**——V6.1b 必须在 512K+ 和 V5.9 开放别名数据上复测。

## 测试范围

- 编码器：冻结 V6.0 L2 C-Former（seed 601 检查点），64 维归一化向量，**未重新训练**；
- 规模：16,384 / 32,768 / 65,536 对象，每规模 4 个固定世界（seed 60/161/262/363）；
- 查询：每世界 96 条 heldout(fold=4) 已知查询（变体 i%6）；另加 64K 上 V6.0b 盲测 16 条已知查询复测；
- IVF：k-means（torch，余弦分配，12 轮），簇数 = max(256, scale/64)；nprobe ∈ {16, 32, 64, 128}；粗召回 Top-256 → 候选内精确精排 Top-1；
- 对照：同一查询的暴力全量精确扫描（批处理）Top-1。

## 核心结果（4 世界均值）

| 规模 | nprobe | Recall@256 | ANN Top-1 | 精确 Top-1 | Top-1 下降 |
|---:|---:|---:|---:|---:|---:|
| 16K | 16 | 100.00% | 100.00% | 100.00% | 0.00 |
| 32K | 16 | 100.00% | 100.00% | 100.00% | 0.00 |
| 64K | 16 | 100.00% | 100.00% | 100.00% | 0.00 |
| 64K | 32 | 100.00% | 100.00% | 100.00% | 0.00 |
| 64K | 128 | 100.00% | 100.00% | 100.00% | 0.00 |

所有 (规模, nprobe) 组合的 Recall@256 均为 1.0（最小=最大=1.0），ANN Top-1 与精确 Top-1 完全一致。

64K 延迟（向量化搜索，seed 601，单世界 96 查询，`bench_v61_latency.py`）：

| nprobe | Recall@256 | ANN Top-1 | p50 ms | p95 ms | p99 ms |
|---:|---:|---:|---:|---:|---:|
| 16 | 100% | 100% | 2.22 | 3.24 | 3.5* |
| 32 | 100% | 100% | 2.01 | 3.13 | 3.48 |
| 64 | 100% | 100% | 2.04 | 2.94 | 3.28 |

`*` p99 首查询含 CUDA 预热（单次 50 ms 离群值），稳定后 p99 ≈ 3.3–3.5 ms。精确批处理扫描 0.26 ms/条（96 条批），ANN 逐查询 2–3 ms——口径不同，见 §6。

盲测复测（64K，nprobe=64）：精确 Top-1 93.75% = ANN Top-1 93.75%，Recall@256 = 100%——ANN 对 V6.0b 盲测结果**零影响**。

## 存储

| 格式 | 64K 字节数 | 每对象 | 说明 |
|---|---:|---:|---|
| FP16 | 8,388,608（8.0 MiB） | 128 B | 满足路线图「512K FP16 ≤64 MiB」折算（512K×128 B=64 MiB） |
| INT8 | 4,325,376（4.1 MiB） | 66 B | int8 主体 64 B + FP16 每向量 scale 2 B；512K 折算 33 MiB ≤ 32 MiB 目标的 103%（接近） |

INT8 在 512K 时会超出 32 MiB 预算约 0.5 MiB，V6.1b 可改为全局/分块 scale 或去掉 scale 列（向量已归一化，可固定 scale=1/127 量化，省 2 B/向量）。

## 质量闸门

| 闸门 | 目标 | 结果 |
|---|---|---|
| ANN Recall@256 | ≥ 99.5% | ✅ 100% |
| ANN+精排 Top-1 相对精确下降 | ≤ 0.5pp | ✅ 0.00pp |
| FP16 @64K ≤ 8 MiB | ✅ | ✅ 8.0 MiB |
| 增量/墓碑/快照回滚 | 单元测试 | ✅ 6/6 |
| 盲测集不受 ANN 影响 | 与精确一致 | ✅ 93.75% = 93.75% |

## 延迟：实现开销 vs 结构优势（诚实报告）

第一版 `search()` 用 Python 列表推导逐元素过滤墓碑与查 id，64K/nprobe=16 时 **p50≈43 ms**——比批处理精确扫描还慢。向量化（`torch.isin` 墓碑过滤 + `id_array` 张量索引）后降到 **p50≈2.2 ms / p95≈3.2 ms**（单元测试 6/6 保持通过），IVF 建索引 7.5 s、增量写入 0.02 s。两点必须说明：

1. 精确扫描的 0.26 ms/条是**批处理** GPU 矩阵乘法；ANN 是**逐查询**路径（每次调用多次小 kernel + 同步）。两者口径不同，直接比较会误导；
2. 即使逐查询，nprobe=16 的候选打分只有约 1K×64 的矩阵乘，结构上是次线性的；2–3 ms 中大部分是逐查询调用开销，V6.1b 的批量查询路径、CUDA graph 或 faiss HNSW 才是 1M 规模延迟的答案。

## 失败/边界

- IVF 在结构化组合空间过于容易（100% Recall 无区分度），必须用真实/开放别名向量在 V6.1b 复测，否则 Recall 指标没有压力；
- INT8 在 512K 略超 32 MiB 预算（103%），需固定 scale 方案；
- 增量建索引（新对象归簇 + 快照切换）已具备 API，但未做并发写入与故障注入测试；
- 与 V5.8 磁盘层（精确别名 B-tree → FTS → ANN）的串联是 V6.1b 的集成工作，本版本未包含。

## 复现

```powershell
D:\conda\envs\cformer-gpu\python.exe -m pytest tests/test_cformer_v61.py
D:\conda\envs\cformer-gpu\python.exe evaluate_v61.py   # 全矩阵正确性（Recall/Top-1/存储）
D:\conda\envs\cformer-gpu\python.exe bench_v61_latency.py  # 64K 延迟微基准
```

原始结果：`artifacts/v61_results.json`（全矩阵）与 `artifacts/v61_latency.json`（延迟）。
