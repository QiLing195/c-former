# V5.7 噪声文本与多跳 Transformation 测试报告

## 实验设置

- 2,048 / 8,192 / 32,768 个独立文本候选；
- 每级 5 个独立世界，每世界 100 个查询；
- 3 个训练种子（401–403），每种子 300 步；
- 文本类型：规范名称、登记别名、Unicode/空格/标点噪声、长上下文干扰；
- C-Former 与 Evidence-RAG 参数均为 29,001；
- 输入特征为 256 维词法哈希加 8 维规范对象键；
- 神经模型只处理经身份、权限、时间和版本控制后的 Top-64。

## 主要结果

| 规模 | C-Former | Evidence-RAG | 差值 | 差值 95% CI | 无观测点 | 纯词法 Top-1 | 混合 Recall@64 | 文本缓存 |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 2,048 | 100.00% | 99.73% | +0.27pp | [-0.08, +0.61]pp | 23.13% | 77.60% | 100% | 0.516 MB |
| 8,192 | 100.00% | 99.20% | +0.80pp | [+0.20, +1.40]pp | 26.47% | 61.20% | 100% | 2.063 MB |
| 32,768 | 99.87% | 97.47% | +2.40pp | [+1.02, +3.78]pp | 25.07% | 32.60% | 100% | 8.250 MB |

32K 下规范名称、登记别名和 Unicode 噪声均为 100%，长上下文为 99.20%。受控多跳路由的正常两跳、循环、深度限制、权限拒绝和版本变化共 5/5 通过。

## 关键分析

1. **文本哈希不能独自承担身份**：纯词法 Top-1 随规模扩大明显下降，在 32K 只有 32.6%。
2. **规范对象键解决身份碰撞**：V5.5 已解析的对象身份以 8 维键注入，Top-64 召回保持 100%；这比继续增大神经参数更有效。
3. **神经层负责视角，不负责边界**：关闭观测点后约为四视角随机水平。权限、时间、冲突、Constraint 和写回验证仍由确定性层执行。
4. **门控优势随难度出现**：2K 时 C-Former 与 RAG 没有统计显著差异；32K 时 C-Former 提升 2.4 个百分点且置信区间高于 0。
5. **长文本重复已去饱和**：重复背景 n-gram 只计一次，防止无关段落凭频率淹没目标锚点。

## 修正过程

早期原型曾出现 32K 准确率降到约 15%–23%。分解指标后确认 Recall@64 已为 100%，失败来自两处：随机负例训练与测试硬负例不一致，以及把规范对象身份埋在 256 维文本哈希中。V5.7 最终版改为词法硬负例训练，并把对象键与文本特征分离，消除了规模退化。

## 限制

- 登记别名和噪声文本都包含可解析的对象编号或已知身份链接；这不是开放世界语义实体发现；
- 字符哈希只能处理词法接近，不能理解真正不同表述的同义关系；
- 多跳测试是受控 Transformation 路由，不是自由语言规划；
- 缓存仍随对象数量线性增长，32K 文本特征缓存为 8.25 MB。

## 下一步

下一阶段应测试“无编号、未登记、语义不同但指向同一对象”的描述对齐，并引入小型预训练文本编码器或蒸馏表示；同时建立错误对象合并的回滚机制。只有身份精度、误合并率和历史版本安全同时达标，才允许自动扩展 Object。

原始结果位于 `artifacts/v57_results.json`。
