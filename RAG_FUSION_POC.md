# C-Former × RAG 融合 POC（路径 A：检索前权限闸门）

日期：2026-09。验证目标：**C-Former 的确定性权限 mask 能否挡在 RAG 前面，让不可见对象在检索前就被排除——"原理上答不出"而非"答了再过滤"。**

## 1. 核心结论

| 指标 | 检索层 POC（POC-1） | 生成层 POC（POC-2，接 DeepSeek API） |
|---|---|---|
| 场景 | 学生问 6 个敏感问题（目标=闭源模型，学生不可见） | 同左 |
| 无闸门泄漏 | **5/6（83%）** | **4/6（67%）** |
| C-Former 闸门后泄漏 | **0/6** | **0/6** |
| 闸门后行为 | 5 次 DENIED（权限拦截） | 5 次 "根据现有资料无法回答" |

**两层证据链完整**：检索层拦截（不可见对象不进候选）+ 生成层不泄密（LLM 看不到机密，自然答不出）。

## 2. POC-1：检索层（`rag_permission_poc.py`）

### 场景
- 知识库：AI 模型域 273 对象（模拟校园知识库文档，每对象=一份文档）；
- 角色：学生（可见 90 个开源对象）/ 管理员（273 全部）；
- 敏感查询 6 条：OpenAI 闭源旗舰 / Anthropic Opus 4.8 / Gemini 3.5 Pro / Qwen3.7-Max / 豆包 1.5 Pro / Grok 5——目标均为闭源（学生不可见）。

### 方法
- RAG 检索：V6.0 神经编码器 查询×库 → Top-1；
- 融合前：检索全部对象（无闸门）；
- 融合后：`mask = -inf; mask[visible] = scores[visible]`——不可见对象分数置 -inf，检索不到；
- 目标不可见 → 判 DENIED（拒绝），不是硬答无关对象。

### 结果（`artifacts/rag_permission_poc.json`）
```
leak_before_gate: 5   泄漏 5/6（83%）——GPT-5.2/Opus 4.8/Gemini/豆包/Grok 全被检索到
leak_after_gate:  0   零泄漏
denied_after_gate: 5  5 次正确拦截为 DENIED
mask_caught: 5        确定性 mask 实际挡住的命中
```
第 6 条（Qwen3.7-Max）无泄漏：神经检索命中"可见的 Llama 4"→ 闸门放行——**权限语义正确（可见就放行），即使检索不准也不误杀**（准确率归检索层，权限归闸门层，职责分离）。

### 复现
```powershell
D:\conda\envs\cformer-gpu\python.exe rag_permission_poc.py
```

## 3. POC-2：生成层（`rag_permission_poc_llm.py`，接 DeepSeek API）

### 场景与方法（同 POC-1）
- 检索 Top-3 文档 → 拼 prompt → DeepSeek `deepseek-chat` 生成；
- 融合前：Top-3 取全量（含不可见对象内容）；融合后：Top-3 只含可见对象；
- 泄漏判定：生成文本中出现不可见对象名（GPT-5.2 / Claude / Gemini / 豆包 / Grok）。

### 结果（`artifacts/rag_permission_poc_llm.json`）——LLM 实际回答

| 查询 | 无闸门（LLM 看到机密） | C-Former 闸门（只看可见文档） |
|---|---|---|
| OpenAI 最新闭源旗舰？ | "是 **GPT-5.2**" ← **泄漏** | "根据现有资料无法回答" |
| Gemini 3.5 Pro 是闭源的吗？ | "是闭源模型" ← **泄漏** | "无法回答" |
| 豆包 1.5 Pro 推理评测？ | 确认"推理突出" ← **泄漏**（暴露对象存在） | "无法回答" |
| Grok 5 何时发布？ | "于 **2026 年**发布" ← **泄漏** | "无法回答" |

### 复现（需要 DeepSeek API key）
```powershell
$env:DEEPSEEK_API_KEY = "sk-..."
D:\conda\envs\cformer-gpu\python.exe rag_permission_poc_llm.py --dry-run   # 先看 prompt，不花钱
D:\conda\envs\cformer-gpu\python.exe rag_permission_poc_llm.py             # 正式（12 次 API 调用）
```

## 4. 架构与原理

```
用户问题（学生身份）
  → C-Former 理解层：意图 + 权限判定
  → 权限 mask（确定性）：不可见对象分数 → -inf（检索前）
  → RAG 检索：只在可见对象里找
  → LLM 生成：只基于可见文档（机密不在候选，自然答不出）
  → 审计：谁问的 / 权限判定 / 检索依据 / 是否被 mask 拦截
```

核心：**mask 在检索之前**——不可见对象不进入候选集，无论神经检索分数多高、LLM 多会编，都"看不到"机密。对比事后过滤（RAG 答完再删），这是原理级保证。

## 5. 边界与诚实声明

1. **权限规则来源**：POC 用"证据文本含'闭源'→不可见"做判定。真实系统权限来自业务规则（角色/组织/密级），需从数据治理层提供——机制相同，规则来源不同；
2. **暴露的数据标注问题**：数据里存在矛盾标注（Apple 系列标"开源"但 Apple Foundation Model 实为闭源；Qwen3.7-Max 属性文本"是开源模型，闭源国产旗舰第一"）——**POC 顺带验证了权限判定不能依赖单一字段**，需内容声明 + 治理规则交叉；
3. **只拦实体/权限级泄漏**：LLM 若编造一段不含可识别实体的话，闸门校验不到（那是生成层幻觉治理的范畴，另一课题）；
4. **理解层跨域需重做词表**：本 POC 在 AI 模型域（词表已适配）。换到制度/流程域（学生守则、审批细则），理解层意图词表需按域重建（零样本迁移不成立的实测边界）；
5. 知识库 273 对象规模小，检索是"廉价全查"——真实大库（万级文档）需 ANN，权限 mask 在 ANN 前的实现（per-centroid 过滤）是工程化课题。

## 6. 对项目的意义

1. **toB 叙事成立**：企业 RAG 的权限泄漏痛点，C-Former 提供"检索前确定性闸门"——不是过滤，是原理上不可达；
2. **可演示**：同一学生、同一批问题，无闸门 67-83% 泄漏 vs 闸门后 0 泄漏 + "无法回答"，直观可复现；
3. **职责分离得到验证**：权限（确定性闸门）与准确率（神经检索）解耦——闸门不误杀可见对象，即使检索不准；
4. **下一步**：真实域测试（制度/流程文档）、ANN 规模化的 mask 实现、权限规则与治理系统的对接。
