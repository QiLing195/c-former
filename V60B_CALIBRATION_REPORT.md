# C-Former V6.0b 阈值校准报告

## 结论

用与盲测集**分离**的校准集（240 已知 + 24 错字 + 5 歧义 + 19 未知）对冻结 V6.0 L2 编码器做阈值校准，得到两个独立结论：

1. **覆盖率可恢复**：冻结 `margin=0.08` 过于保守——盲测已知查询只有约 4–10/16 被判 supported（覆盖率低）。校准到 `margin≈0.02–0.04` 可把已知支持率恢复到约 12–16/16，同时已知+错字的排名风险 ≤ 2%；
2. **单一全局 margin 会误支持**：放宽 margin 后，盲测集里 2–3 条歧义、1–2 条未知、1–2 条冲突查询被误判为 supported（本应拒绝）。**必须按查询类型分校准**，而不是用单一全局阈值。

另有一个**阈值无法解决的问题**必须说清：盲测已知 Top-1 = 93.75% 是**排序错误**（3 条查询把错误对象排到第一），阈值校准只改变「supported/ambiguous」的判定，**不改变排序**。所以校准解决的是覆盖率—风险权衡，不是那 3 条排序失败（后者需编码器能力，V6.0c 数据增强已证明无效）。

## 校准集（与盲测集分离）

- 已知：12 条盲测句式 × 20 个**新的** fold-4 对象（排除盲测集 target）= 240 条；
- 错字：24 条（内容字单字 OOV 替换）；
- 歧义：5 条同名只提名称；
- 未知：19 条 OOV 密集 / 缺信息文本；
- 全部通过无编号/无模板片段检查；target 与盲测集不重叠（单元测试断言）。

## 风险—覆盖率扫描（3 种子，已知+错字）

| 种子 | 建议 margin | 覆盖率 | 风险 |
|---:|---:|---:|---:|
| 601 | 0.02 | 94.7% | 0.8% |
| 602 | 0.02 | 89.0% | 1.7% |
| 603 | 0.04 | 72.3% | 0.0% |

冻结 `margin=0.08` 在盲测文本上覆盖率仅 28.8%（V6.0b 报告）；校准后同一编码器可恢复到 72–95% 且风险 ≤ 2%（对已知+错字的排名风险）。

## 盲测集在新旧阈值下的支持数（每种子，/16 已知）

| 类别 | 冻结 0.08 | 校准 0.02–0.04 |
|---|---:|---:|
| known supported | 4–10 | 12–16 |
| hard supported | 0 | 1–2 |
| disambiguated supported | 1–2 | 2–3 |
| **ambiguous 被误支持** | 0 | 2–3 ⚠️ |
| **unknown 被误支持** | 0 | 1–2 ⚠️ |
| **conflict 被误支持** | 0–1 | 1–2 ⚠️ |

校准阈值恢复了已知覆盖率，但代价是把部分「应拒绝」的歧义/未知/冲突查询放行。风险指标若只统计已知+错字的排名错误，会低估真实风险（误支持也是风险）。

## 建议：按类型分校准

| 查询类型 | 建议 margin | 理由 |
|---|---:|---|
| 完整已知（四证据齐全） | 0.02–0.04 | 覆盖率优先，排序风险低 |
| 错字 / 口语 | 0.02–0.04 | 与已知同档，但需单独跟踪 |
| 同名歧义 | 保持 0.08 或更严 | 宁可不答，不得误支持 |
| 未知 / 缺信息 | 保持 0.08，另加 coverage 收紧 | 误支持不可接受 |
| 冲突（双候选） | 保持 0.08，另加「双候选 margin」特判 | 二选一不得当确定答案 |

实现上把 `EvidenceVerifier` 从「单一 margin」改为「按证据完整度/类型查表」，并在独立验证集上报告三类曲线：支持率—覆盖率、误接受率、审核量。

## 复现

```powershell
D:\conda\envs\cformer-gpu\python.exe -m pytest tests/test_cformer_v60b_calibration.py
D:\conda\envs\cformer-gpu\python.exe calibrate_v60.py
```

原始数据见 `artifacts/v60b_calibration.json`。
