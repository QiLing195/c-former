# 跨域实验报告：零样本迁移 vs 多域联合训练（三域版）

## 结论

用「AI 大模型」「国家」「电影」三个完全不同的域验证小编码器的跨域能力：

| 训练方式 | AI 域 | 国家域 | 电影域 |
|---|---:|---:|---:|
| **单域训练**（只训 AI） | 42.4% | **5.2%**（≈随机） | — |
| **2 域联合**（d=128） | 36.7% | 60.0% | — |
| **3 域联合**（d=256） | **46.5%** | **67.9%** | **74.9%** |
| 随机基线 | 0.4% | 1.5% | 1.7% |

1. **零样本跨域迁移不成立**：单域训练后，新域解析 ≈ 随机水平（国家 5.2%）；
2. **多域联合训练在多域都有效**：3 域联合下，三个域全部远高于各自随机基线；
3. **容量提升让各域共同受益**：d=128→d=256，AI 36.7%→46.5%、国家 60%→68%——「容量够则多域不掉」成立；
4. **难度排序**：电影（74.9%）> 国家（67.9%）> AI（46.5%）——名称独特性决定难度（电影名最独特、AI 版本号最相似）；
5. 诚实对比：AI 域 46.5% 低于单域全批的 76.3%，主要因 minibatch 欠训练（样本数差 ~20 倍），非联合训练本身；公平验证需更多步数。

## 实验设计

- 数据：AI 模型域（273 对象）+ 国家域（68 对象）+ 电影域（60 对象），合并共享 tokenizer + 域标签；
- 训练：小批量（64）、d=64/128/256、300 步、3 种子；单域 vs 联合（--train-domain all）；
- 评测：`evaluate(domain=...)` 分域报告；identity_top1 = name+alias；
- 随机基线：各域 1/对象数。

## 关键观察

1. 域内性能随容量提升，但跨域性能纹丝不动（单域训练时国家 4.4→5.2%）——域内学习不带来零样本迁移；
2. 联合训练时各域共享容量，d=128 时 AI 域略降（42.4%→36.7%），d=256 时全部回升——容量是联合训练的关键变量；
3. 边界指标（歧义/未知）在联合模型上依然稳定（未知零误支持 80-100%）。

## 对项目的影响

1. **共享对象世界概念成立且可扩展**：一个对象库容纳三个域（AI/国家/电影）无冲突，联合训练后各域都可用；
2. **编码器是域特定的，但可多域联合训练**：零样本开放世界解析超出小编码器能力，多域联合训练是让共享世界多域可用的正确方式；
3. **第二、三域数据的价值**：揭示了「需要多域训练」与「容量决定多域表现」两个架构真相；
4. **未来方向**：①更多步数/更大容量的多域复测，②统一证据槽位语义以增强域间共享，③或接受「逐域/联合训练」定位。

## 复现

```powershell
D:\conda\envs\cformer-gpu\python.exe build_ai_models_dataset.py
D:\conda\envs\cformer-gpu\python.exe build_countries_dataset.py
D:\conda\envs\cformer-gpu\python.exe build_movies_dataset.py
# 3 域联合（d=256）：
D:\conda\envs\cformer-gpu\python.exe train_eval_cross.py --steps 300 --seeds 1 2 3 --d-model 256 --train-domain all
```

结果：`artifacts/cross_domain_joint3_d256.json`。
