# V6.3 ANN 规模化检索 POC 报告

> 目标：回答 toB 客户必然会问的那句——"**十万条制度还查得出来吗？**"
> 本文所有数字均为本机实测（哈希占位编码器），**负结果与限制条件完整保留**。

## 一句话结论

**权限分区（跨级零泄漏）完全成立；ANN 忠实度可以校准到目标值，但代价随规模变化明显。**
在 5 万条规模上，IVF 扫描 14.9% 的数据即可达到 **0.9872** 的忠实度（相对暴力精确检索），
比暴力快 **15.7×**；而在 1 万条规模上，同样达标需要扫描 **33.6%**，**基本不值得用 IVF**。

⚠️ **语义检索质量尚未测得**：本轮用的是无语义能力的哈希占位编码器。
真实语义召回必须配 ONNX int8 模型后重测，**在此之前本报告不代表检索质量**。

---

## 一、为什么权限必须"结构性分区"，而不是"检索后再过滤"

这是本次唯一完全成立、且与编码器/语料无关的结论。

朴素做法是：全局 ANN 检索 → 按权限过滤结果。它在治理场景下有三个问题：

1. **索引文件本身含全部密级内容** → 索引泄漏 = 全库泄漏；
2. **受限条目挤占 top-k 名额** → 权限越低召回越差，且差多少不可预测；
3. **命中数与耗时随密级变化** → 时序/结果侧信道可反推"受限内容里有没有这条"。

因此 `cformer_v63/ann_index.py` 按「可见该内容所需的最低级别」**物理分区**：
级别 L 的查询只遍历 `level ≤ L` 的分区，`level > L` 的分区**在代码路径上不可达**。

> 泄漏不是"过滤掉"的结果，而是"没有路径能到达"的结果。
> 这与 GovLayer 既有的"检索前 mask"是同一个哲学，也是本项目最核心的卖点。

分区按 **level** 而非 role 命名：level（0 最低）跨域通用，role 名各库不同
（员工/学生/孩子）。同一索引可服务多套角色命名——这也是 `server/auth.py` 里
"令牌 → 级别 → 角色"能用一套逻辑的根本原因。

单个对象在多个级别都有内容时（如"考勤处罚"在 0/1/2 都有条款），
每个级别各建一条索引项，文本是**该级别下的累计可见内容**，
保证"检索到的"与"该角色能看到的内容"一致，不出现"检索到却展示为空"的错配。

---

## 二、实测环境与配置

| 项 | 值 |
|---|---|
| 语料 | 7 条真实制度条款 + 1 条仅 level 2 的"高管薪酬明细"（秘密条款）+ 合成干扰条款 |
| 规模 | 10,000 / 50,000 条 |
| 查询 | **47 条**去重后保真度问题（用于 recall）+ 7 条手写真实制度问题（用于语义对照） |
| 编码器 | `hashing-blake2b`，dim=256，**无语义能力（结构占位）** |
| top-k | 5 |
| 目标召回 | 0.95 |
| 质心数 | `sqrt(条目数) × cluster_multiplier` |

**为什么查询要用 47 条而不是 7 条**：7 条问题上算 recall@5，粒度是 1/35，
"1.0"这种数字几乎没有统计意义。样本量不足会让报告显得漂亮而不可信。

---

## 三、结果

### 3.1 权限：跨级泄漏 = 0（唯一完全成立的结论）

| 规模 | 质心系数 | 泄漏数 |
|---|---|---|
| 10,000 | 1.0 / 4.0 | **0** |
| 50,000 | 1.0 / 4.0 | **0** |

在所有规模、所有 n_probe、所有质心配置下均为 **0**，包含专门设置的
`secret-salary`（最低可见级别 = 2）跨级探测：级别 0 与级别 1 的查询
在任何配置下都未能命中它。

**这是代码路径的性质，不依赖编码器或语料，因此换 ONNX 模型后不会改变。**

### 3.2 忠实度与代价（质心系数 4.0）

| 规模 | 校准 n_probe | 质心数 | 判定召回 / 重测召回 | 扫描比 | ANN ms | 暴力 ms | 加速 |
|---|---|---|---|---|---|---|---|
| 10,000 | 128 | 386 | 0.9617 / 0.9617 | **33.6%** | 1.98 | 7.03 | 3.5× |
| 50,000 | 128 | 864 | 0.9872 / 0.9872 | **14.9%** | 4.30 | 67.34 | 15.7× |

**关键读数不是"0.95 达标"**——那是校准的目标，属构造性结果。
真正有信息量的是**达标的代价**：1 万条要扫掉三分之一，5 万条只要扫掉七分之一。

### 3.3 质心系数是关键旋钮（对照组，系数 1.0）

| 规模 | 校准 n_probe | 质心数 | 召回 | 扫描比 | 加速 |
|---|---|---|---|---|---|
| 10,000 | 32 | 96 | 0.9489 | 32.6% | 5.1× |
| 50,000 | 128 | 216 | 0.983 | **60.0%** | 5.2× |

5 万条时系数 4.0 只需扫描 14.9%，系数 1.0 要扫 60.0%——**同样达标，代价差 4 倍**。
原因是 `sqrt(n)` 的质心数相对 n_probe 太粗：系数 1.0 时 n_probe=128 已接近探遍全部
216 个质心，IVF 退化成"带排序的全量扫描"。

**注意 10k 档两个系数几乎等价（3.5× vs 5.1×）**：质心加密只在规模变大后才有收益。

### 3.4 语义质量：数据缺失，且原因明确

| 指标 | 值 |
|---|---|
| 暴力精确检索的针命中 | **4/7** |
| ANN 最佳针命中 | **4/7** |

**暴力精确检索自己也只有 4/7**，说明缺的 3 条是**编码器语义弱**所致，
与索引无关。这正是评测脚本必须同时打印"暴力基准针命中"的原因：
不打印它，就会把编码器的锅算到索引头上（或反过来）。

哈希编码器只能捕捉字符级重叠（"旷工"与"旷工"），对"旷工 ↔ 缺勤"完全无感。

---

## 四、负结果与限制（不许省略）

1. **语义检索质量未测**。本轮编码器无语义能力，本报告**不能**用来宣称检索质量。
2. **合成语料会高估 IVF**。干扰条款来自 40 词小词表，人为制造强簇
   （实测 separation 0.54~0.58），真实语料的簇结构更弱。实测数字应以真实制度语料复核。
3. **延迟方差大，不应采信单次 wall-clock**。同一配置（10k、n_probe=128）两次运行
   分别测得 1.241ms 与 1.982ms，相差 60%；暴力检索三次为 68.66 / 66.84 / 71.31ms。
   **稳定可比的是扫描比**（确定性计数），wall-clock 只应作数量级参考。
4. **0.95 忠实度意味着约 5% 的 top-5 偏差**。对一个卖"答得对、不越权"的系统，
   漏掉掌管答案的那一条制度是**产品风险，不是技术细节**——是否接受必须由业务决定。
   本 POC 不替客户做这个决定。
5. **ANN 尚未接入服务**。目前是独立 POC（`eval_ann_index.py` + `cformer_v63/ann_index.py`），
   `GovLayer.answer()` 仍走关键词 + LLM 语义路径，两者**尚未打通**。
6. **索引未做持久化**。每次启动重建：10k 约 1.6s，50k 约 15.2s。生产需落盘 + 校验版本一致性
   （制度是有版本和生效期的，索引必须与制度版本绑定）。
7. **单进程内存索引**。未做多进程共享、增量更新、删除（制度条款会废止）。

### 方法学教训（留档）

评测脚本自身出过一个 bug：48 条保真度问题串里出现 **1 条重复**（去重后 47 条），
而暴力基准是 `dict`（重复 key 被折叠），导致
`calibrate` 按"含重复列表"算均值、`measure` 按"去重后 dict"算均值——
两个分母不同，输出"**判定召回 0.95 达标、打印召回 0.9489 不达标**"的自相矛盾结果。

**这类 bug 的性质与"默认值 n_probe=2 只有 0.31 召回"完全相同：专门制造
"看起来达标、其实没达标"的假象。** 现已修复（去重 + 打印"判定值/重测值"+ 一致性断言），
并由 `tests/test_ann_index.py` 中的不变量测试钉死。

---

## 五、工程建议

基于实测，**不建议无条件启用 ANN**：

| 规模 | 建议 | 理由 |
|---|---|---|
| ≤ 10,000 条 | **直接用暴力精确检索** | 7ms，召回恒为 **1.0**，无近似风险，代码路径更简单 |
| ~10,000–50,000 | 校准后可用，但收益有限 | 需扫 1/3 数据才达 0.96，10k 档基本不值 |
| ≥ 50,000 条 | **ANN 校准启用** | 扫 15% 达 0.987，15.7×；规模越大收益越明显 |

必须遵守的三条：

1. **`n_probe` 一律校准，不许写死常数。** 默认给 2 会静默丢掉约 2/3 命中
   （实测 1 万条档忠实度 0.31），而系统看起来一切正常。这是最危险的一类缺陷。
2. **暴力检索路径必须保留**。它是召回基准，也是小规模下的正确选择，
   更是"ANN 是否可靠"的唯一验证手段。
3. **索引必须与制度版本绑定**。制度有生效期与废止，索引漂移会让权限与内容错配。

---

## 六、未完成事项

- [ ] **配 ONNX int8 模型重测语义召回**（`requirements-ann.txt` + `GOVLAYER_ONNX_MODEL`）
- [ ] 用**真实制度语料**复核（当前为合成干扰条款，会高估 IVF）
- [x] 权限分区结构性零泄漏（已验证）
- [x] `n_probe` 校准 + 不达标如实上报
- [ ] 索引持久化 + 与制度版本绑定
- [ ] 接入 GovLayer 检索路径（当前未打通）
- [ ] 增量更新 / 条款废止
- [ ] 真实语料上的 recall/OOV 复核

---

## 附：复现命令

```powershell
# 结构与权限（无需模型，秒级）
python -m pytest tests/test_ann_index.py -q
python eval_ann_index.py --sizes 10000 50000 --cluster-multiplier 4

# 真实语义召回（需 ONNX int8 模型）
pip install -r requirements-ann.txt -i https://pypi.org/simple
$env:GOVLAYER_ONNX_MODEL="E:\deepseek\c-former\models\bge-small-zh-v1.5-int8"
python eval_ann_index.py --sizes 10000 50000 --cluster-multiplier 4
```

明细数据：`artifacts/ann_eval.json`（不入库）

---

**结论一句话**：权限结构性零泄漏是**真的**，而且与规模无关；
ANN 是**可用的但需要校准的优化**，在 5 万条以上才值得引入；
而"AI 能不能查得准"这个问题，**本轮数据回答不了**——它取决于编码器，不取决于索引。
