# V5.8 开放别名候选与分层存储设计

## 为什么不是指数存储

对象、别名和有界文本特征的存储量近似线性：`O(objects + aliases + postings)`。容易失控的是朴素查询 `O(queries × objects)`、全量向量加载，以及为每个观察视角复制一份对象向量。

## 分层结构

```text
已验证别名 -> SQLite B-tree 精确查询
未知表述   -> FTS5 内容空倒排索引 -> Top-64
Top-64     -> mmap int8 对象向量 -> 候选别名沙箱
观察视角   -> 小型 scope 记录，查询时动态组合
```

- 对象文档只保存一次；
- 四个观察视角只保存四条小记录，不复制向量；
- 词法倒排每对象最多 48 个哈希 term，防止长文本无限扩张；
- 256/128 维向量按对象逐行 int8 量化，外加一个 float32 scale；
- 向量文件使用 `numpy.memmap`，只读取候选行；
- FTS5 使用 `content=''`，倒排索引不重复保存原始 term 文本；
- 版本更新采用追加记录和撤回版本，不复制完整快照。

## 开放别名安全

未知表述的第一候选始终写入 `alias_candidates(status='proposed')`，不会自动进入正式 alias 表。人工或外部证据审核后才能 `verified`；拒绝或错误合并可以通过版本化撤回恢复历史状态。

## CUDA位置

CUDA只负责批量编码、候选向量评分和 Top-64 神经重排。SQLite、历史版本和大对象库留在 CPU/磁盘。这样 4GB 显存不需要容纳整个世界，规模主要受磁盘和索引构建速度限制。
