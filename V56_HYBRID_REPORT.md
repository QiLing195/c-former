# V5.6 神经—结构化联合测试报告

## 实验设置

- 规模：2,048 / 8,192 / 32,768 个独立 ObjectState/Transformation 候选；
- 每级 5 个独立世界，每世界 200 个查询；
- 5 个训练种子（301–305），每模型每种子 600 步；
- 结构化对象分片保证正确对象的四个视角进入候选，再加入硬负例形成固定 Top-64；
- C-Former 与 Evidence-RAG 均为 15,361 参数，初始化、批次和训练预算相同；
- 权限、时间、版本、冲突和写回仍由 V5.5 确定性控制器负责。

## 结果

| 候选规模 | C-Former | Evidence-RAG | 配对差值 | 差值 95% CI | 无观测点 | Recall@64 | 重排延迟 | 全量神经扫描 | 紧凑缓存 |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 2,048 | 96.98% | 77.36% | +19.62pp | [+6.66, +32.58]pp | 26.36% | 100% | 0.026 ms | 0.007 ms | 0.094 MB |
| 8,192 | 97.58% | 76.66% | +20.92pp | [+6.53, +35.31]pp | 25.06% | 100% | 0.025 ms | 0.022 ms | 0.375 MB |
| 32,768 | 97.54% | 76.70% | +20.84pp | [+6.84, +34.84]pp | 24.66% | 100% | 0.027 ms | 0.067 ms | 1.500 MB |

最终精确均值和置信区间由 `aggregate_v56.py` 生成并保存在 `artifacts/v56_summary.json`，避免报告手工舍入被当成原始数据。

## 分析

1. **规模稳定**：C-Former 从 2K 到 32K 没有下降，原因是对象索引先把合法空间压到固定 64 个候选；神经计算不随世界规模线性增长。
2. **观测点确实必要**：关闭观测点后约为四视角随机水平 25%。
3. **门控交互比加法条件更稳定**：Evidence-RAG 对初始化非常敏感，不同种子从约 59% 到 94%；C-Former 各种子均约 96%–99%。任务需要对象状态、问题类型和观测点三者交互，单纯加法难以稳定表达。
4. **小世界不应强行使用两阶段检索**：2K 时全量矩阵扫描反而更快；8K 接近；32K 时 Top-64 重排约比全量神经扫描快 2.5 倍。系统应根据规模选择执行路径。
5. **缓存仍线性增长，但常数下降**：32K 的紧凑键/类型/视角缓存为 1.5 MB；若缓存所有 48 维神经向量则约为 6 MB，因此默认只长期保存紧凑索引，按 Top-64 编码精确候选。

## 边界结果

V5.6 没有把边界决策交给神经模型。V5.5 控制器的专项测试继续保持：不安全模拟、未来泄漏和身份坍缩均为 0；未经验证的假设仍不能写回正式记忆。

## 限制

- 这是合成对象/Transformation 检索，不是自然语言问答或生成；
- 对象规范身份由结构化层提供，尚未测试开放世界实体发现；
- Recall@64 的 100% 是对象索引的结果，不代表纯语义向量在开放检索上也能达到 100%；
- 延迟是本机 CPU 微基准，不能直接外推到服务部署。

## 下一步

进入噪声文本适配：把对象名、别名、状态描述和 Transformation 文本编码成候选，保留同一确定性控制器；重点测试未见别名、描述冲突、长文本证据和多跳 Transformation，而不是继续扩大纯结构化规模。
