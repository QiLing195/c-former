# C-Former V6.2 世界推理块（最小原型）报告

日期：2026-08。前置结论：README《泛化消融（负结果）》证明 dual-encoder 原理上无法做跨候选
比较，"系列最新"类超级指代只能背训练系列（留出集 ≈6.7%）。

## 1. 方案

`cformer_v62/reasoner.py` —— `WorldReasoner`，纯逻辑、无训练参数、无 torch 依赖：

```text
查询含方向词（最新→max；最早/初代/第一代→min；双向同现→放弃裁决）
    → 系列锚定：词法优先（查询显式含"{公司} 的 {系列} 系列"；
       公司级命中作次级锚），神经榜首锚点仅回退
    → 取检索候选中属于锚定系列的成员
    → 单成员：直接成立；多成员：年份极值，平局用 series_index（定义顺序）
    → 输出选择 + 完整可审计轨迹（anchor 来源 + 各成员年份）
```

挂接点：pipeline 重排之后、verifier 之前；任何一步无法裁决（方向冲突/锚定失败/
年份缺失）返回 None 回退原神经路径。符合路线图 §13"确定性控制器优先于神经网络"。

## 2. 调试中确认的两个关键事实

1. **神经榜首连系列都会记错**：留出集查询"OpenAI 的 GPT 系列…"的神经 Top1 是
   minimax-m2-7——锚点若依赖神经排名则垃圾进垃圾出，必须词法锚定（trace 里记录 anchor=lexical）。
2. **单成员系列是特例**："华为 的 盘古 系列…"只有盘古 5.0 一个成员，"成员<2 才比较"
   的守卫会让它落回弱神经路径被误拒——单成员时唯一成员即答案。

## 3. A/B 结果（同一链路/种子/校准阈值，仅开关 WorldReasoner；`artifacts/v62_results.json`）

| 指标 | baseline | + WorldReasoner |
|---|---:|---:|
| 主数据 known Top-1 | 86.0% | **100%** |
| **留出集 known Top-1** | 0%（三种子全 0） | **100%（三种子全 100）** |
| 盲测 known Top-1 | 11.1% | 19.4%（"最早那版GPT"经 min 方向命中） |
| 盲测歧义检出 | 83.3% | 83.3%（无损失） |
| 盲测 unknown 拦截 | 100% | 100%（无损失） |
| reasoned 路径占比 | 0 | 89–100% |

泛化消融暴露的架构缺口**在本测范围内已关闭**：泛化不再依赖记忆，因为成员选择
由证据中的结构化事实（年份/定义顺序）+ 显式锚定完成，轨迹可重放。

## 4. 边界与诚实声明

1. 词法锚定依赖查询显式包含公司/系列名——指代消解（"阿里那个千问"、"它"）尚未覆盖，
   需要别名表与共指层；
2. 年份抽取依赖证据文本含 (19|20)\d{2}；证据缺年份时该查询回退神经路径；
3. "最新/最早"以外的超级指代（最强/最流行）没有结构化判据，仍走神经路径；
4. 本测为 212 对象真实库上的最小原型，不构成对通用推理能力的主张。

## 5. 下一步

1. 指代消解：把别名表接入词法锚定（"千问"→Qwen、"深度求索"→DeepSeek）；
2. 把 reasoner 的选择轨迹接入 CandidateLedger 审计流（reasoned 结果同样只 supported）；
3. 数据扩量后检验词法锚定的歧义率（同名子串、多系列同名词）。