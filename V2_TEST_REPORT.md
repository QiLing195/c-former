# C-Former V2 测试报告

测试日期：2026-07-15  
环境：Python 3.13、PyTorch 2.9 CPU

## 测试目标

验证以下定义：

```text
共享世界 M + 观测点 o + 独立问题 q -> 答案
```

世界记忆先独立编码，观测点只调制查询，不 Mask 或删除任何事实。同一世界的编码可以缓存并服务多个问题。

## 数据与问题

训练时程序化随机生成世界，每个世界包含 14 条事实：

- 4 个人的位置；
- 2 个物品的持有者；
- 4 个观测者分别对 2 个物品位置的认知。

固定测试使用 Orion、Lyra、Cygnus、Draco 四个小世界。每个世界包含 36 个问题，总计 144 个问题。

| 问题类别 | 含义 | 主要验证点 |
|---|---|---|
| true_location_2hop | 物品持有者 -> 持有者位置 | 两跳推理、客观事实 |
| observer_belief | 当前观测者认为物品在哪里 | 观测点相关答案 |
| object_holder | 谁持有物品 | 客观事实 |
| outside_person_location | 查询另一个人的位置 | 使用观测点之外的信息 |

## 模型对照

| 模型 | 参数量 | 世界记忆可缓存 | 使用观测点 |
|---|---:|:---:|:---:|
| Concat Transformer | 172,104 | 否 | 是 |
| Shared Question Only | 201,288 | 是 | 否 |
| Observer-CFormer | 201,288 | 是 | 是 |

Question Only 保留相同容量但关闭观测点路径，用来隔离观测点本身的贡献。

## 500 步主实验

| 模型 | 两跳真实位置 | 观测者认知 | 物品持有者 | 观测点外位置 | 总体 |
|---|---:|---:|---:|---:|---:|
| Concat Transformer | 37.50% | 34.38% | 100% | 100% | 71.53% |
| Shared Question Only | 100% | 25.00% | 100% | 100% | 83.33% |
| Observer-CFormer | 100% | 100% | 100% | 100% | 100% |

Observer-CFormer 在第 200 步已经达到四类任务 100%。四个固定世界的每一类问题均为 100%。

Question Only 的观测者认知准确率为 25%，正好等于四个可能位置的随机水平；其余任务全部正确。这说明共享记忆足以完成客观检索，但观测点是区分不同主体认知所必需的。

Concat Transformer 在 500 步尚未稳定学会两跳和观测者索引，但这不是能力上限。

## 延长拼接基线训练

Concat Transformer 训练到 1,500 步：

```text
step 600:  fixed_mean = 99.31%
step 900:  fixed_mean = 100.00%
step 1500: fixed_mean = 100.00%
```

因此，标准拼接 Transformer 最终也能解决该任务。V2 实验不能证明 C-Former 拥有标准 Transformer 不具备的表达能力，只显示了更符合任务结构的归纳偏置。

## 三随机种子固定预算对照

训练预算固定为 300 步：

| Seed | Concat Transformer | Observer-CFormer |
|---:|---:|---:|
| 21 | 91.67% | 100% |
| 22 | 99.31% | 100% |
| 23 | 100% | 100% |
| 平均 | 96.99% | 100% |

C-Former 在 3/3 次训练中达到 100%；拼接模型能够达到同样上限，但短训练预算下波动更大。

## 信息复用速度

测试为同一个世界的一批 36 个问题。三个随机种子测得的中位 CPU 延迟：

| 模型/模式 | 36 问题延迟 |
|---|---:|
| Concat Transformer，每次重编码 | 8.31 ms |
| Observer-CFormer，不使用缓存 | 7.73 ms |
| Observer-CFormer，复用世界缓存 | 3.90 ms |

缓存相对拼接基线减少约 53% 的批量查询时间。该数字只适用于当前小模型、CPU 和固定批量，不能直接外推到大模型；但它验证了“世界编码一次、多问题复用”的工程路径。

## 性能评估

### 已得到支持的判断

1. 观测点不会阻止模型使用其他主体的信息：`outside_person_location` 为 100%。
2. 客观答案能够跨观测点保持一致：真实位置和持有者问题为 100%。
3. 观测点相关答案能够正确变化：观测者认知问题为 100%。
4. 两轮查询能够完成当前的两跳关系组合。
5. 共享世界缓存能够明显减少多问题重复查询成本。
6. 在相同短训练预算下，C-Former 的收敛比拼接基线更稳定。

### 尚不能得出的判断

1. 不能说 C-Former 的最终准确率必然高于标准 Transformer；延长训练后两者都为 100%。
2. 固定测试世界来自与训练相同的程序化分布，不是自然语言或跨分布测试。
3. 世界只有 14 条结构化事实，尚未验证长上下文检索。
4. 当前答案是 8 类分类，不是开放式文本生成。
5. 尚未测试三跳以上推理、矛盾事实、缺失信息和时间变化。
6. 缓存优势需要在更长记忆和更多连续问题下重新测量。

## 结论

V2 的核心方向值得继续：它没有表现出比 Transformer 更高的理论能力上限，但在“固定世界、多观测点、多问题”场景中展现了合适的结构性优势——观测者认知可控、客观事实稳定、全局信息仍可访问，并且世界表示可以复用。

下一阶段应保持该架构，扩大到带时间顺序、信息可见性和三跳关系的自然语言小世界，同时加入未见实体组合和长记忆压力测试。

