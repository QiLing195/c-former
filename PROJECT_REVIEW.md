# C-Former 项目评审（截至 V6.0）

评审日期：2026-07（基于当前工作区快照）
评审方式：代码走读 + 全量测试运行 + 27 份设计/报告文档核对 + 版本演进追踪

## 1. 项目定位

C-Former（Constellation Transformer）是一个面向验证的「共享对象世界 + 观测点条件化」原型。核心假设有三条：

1. 同一份知识可以存放在共享世界中，被不同问题、不同观测点复用；观测点是坐标/索引，不是知识的副本。
2. 对象身份解析必须先于观测点注入（「先身份、后观测点、再推理」），观测点只能决定可见性与表达方式。
3. 神经网络只能提出 `supported` 候选，正式身份写入、验证、回滚必须由外部确定性治理层（verifier/ledger）控制。

项目自身定位诚实：它是「检索与身份治理层」，不是通用生成式大模型。这个边界在每一份报告里都反复声明。

## 2. 版本演进脉络

| 版本 | 主题 | 关键结论 |
|---|---|---|
| V1 | OP-FiLM 观测点条件化注意力 | 同一输入在不同观测点下可稳定产生不同正确结果（L/C/R 100%） |
| V2–V4 | 共享世界查询、分层记忆、可靠性边界 | 权限/时间/版本/冲突由确定性层负责，规模 2K–32K 稳定 |
| V5.4 | 版本化递归时空对齐 | 递归/时间/空间消融后准确率显著下降 → 三者都是承重件 |
| V5.5 | 最小认知层（Object/Transformation/沙箱） | 模拟与正式记忆隔离，验证后才写回 |
| V5.6 | 神经—结构化联合检索 | 结构化 Top-64 内重排，相对 Evidence-RAG +20pp |
| V5.7 | 噪声文本与多跳 | 修复了「文本哈希埋身份」导致的规模退化 |
| V5.8 | 磁盘优先分层存储 | SQLite+FTS5+mmap int8，131K 对象不复制向量 |
| V5.9 | 无编号开放别名、CUDA 到 512K | 100% Top-1，FP16 缓存 62.5 MiB，仍为 O(N) 扫描 |
| V6.0 | 共享 Token Transformer（中文） | 默认冻结 2 层，64K 100% Top-1/否定准确率 |

演进主线清晰：从「注意力机制验证」逐步走向「真实 Token 文本 + 磁盘存储 + 安全治理」的检索解析系统，且每一步都先解决上一版已确认的问题，没有一次性堆叠功能。

## 3. 优点（有证据支撑）

1. **研发纪律是本项目最大的资产**。每版都有 设计 → 最小实现 → 单元测试 → 小规模闸门 → 4 世界梯度测试 → 对照消融 → 失败修正 → 冻结报告 的闭环；路线图（V60_TO_V65_ROADMAP.md）明确写了质量闸门、回退条件、停止规则和参数/显存预算。
2. **评估诚实**。V6.0 报告明确写出「这不是全面超过 Transformer 的结论」——公平的 2 层平铺 Transformer 同样 64K Top-1 100%；V5.9 报告承认 512K 检索 C-Former 1.293 ms/条慢于 Transformer 0.244 ms/条；多份报告主动指出合成数据饱和、未知拒绝率偏容易等指标弱点。
3. **安全边界工程扎实**。`CandidateLedger` 状态机（proposed→supported→verified→rolled_back）要求显式 reviewer 才能 verify；版本化审计日志；`EvidenceVerifier` 用 score/margin/coverage 三条件保守决策；防泄漏测试（文本无编号、改 label 不改变编码）做到了单元测试级别。
4. **反捷径测试设计好**。V6.0 的否定硬负例刻意构造「字符多重集合完全相同、仅词序不同」的句子对（如「不要赤红需要蔚蓝」vs「不要蔚蓝需要赤红」），并断言平均池化模型无法区分而 Transformer 可以——这是能真正暴露模型能力边界的测试。
5. **可复现性好**。固定种子、固定世界、精确命令、原始 JSON、检查点、失败案例全部保留；V6.0 一次编码计时受桌面任务干扰后还做了独立重测并记录替换原因。
6. **自我修正有记录**。V5.7 的 32K 退化被分解指标定位为「词法哈希 vs 对象身份」问题并修复；V6.0 的否定变换非自反映射 bug 在正式测试前被发现并修正，旧结果未混入报告。

实测：`D:\conda\envs\cformer-gpu\python.exe -m pytest -q` → 54 passed（12.5s）；另有 6 个 error 是测试环境临时目录权限问题（非项目缺陷，见 §5.9）。

## 4. 主要问题

### 4.1 版本控制完全缺失（最严重）
`git status` 显示仓库**没有任何 commit**，全部文件处于 untracked 状态。没有历史、没有版本标签、没有回滚点、没有远端备份。整个项目（代码 + 27 份文档 + 结果）只有当前一个快照——任何误删/误改都不可恢复，与项目内部「版本化、可回滚」的理念自相矛盾。

### 4.2 靠目录复制做版本演进，重复代码多
`cformer`、`cformer_v2` … `cformer_v60` 是整包复制再修改，而非在单一代码库上用分支/标签演进。后果：
- `cformer_v60/model.py` 中 `contrastive_loss` 三个模型（C-Former/MLP/Flat）逐字重复三份，共 60+ 行；
- 平均池化、门控融合等逻辑在 v56–v60 之间反复拷贝；
- 23 个根级脚本（aggregate_v51.py … train_evaluate_v60.py）与 27 份平铺 md 全部堆在根目录，随版本增长只会更乱。

### 4.3 无打包、无 CI、环境不可移植
- 只有两行 `requirements.txt`（`torch>=2.0`、`pytest>=7.0`），无 `pyproject.toml`、无锁文件、无 conda env yml；
- README 与复现命令硬编码 `D:\conda\envs\cformer-gpu\python.exe`；
- 默认 `python`（deepseek env）连 pytest 都没有，换机器/换环境即不可复现；
- 无 lint/format/pre-commit、无 CI 流水线，测试只能靠手动跑。

### 4.4 合成数据饱和，外部有效性未建立
V6.0 报告自己也承认：Top-1/否定准确率全部 100%，未知文本是固定的词表外表达、歧义是缺字段构造，都不能代表真实开放世界。真实中文对象别名、真实同名实体、自然未知表达、真实冲突证据目前为零。V6.0 闸门中「外部有效性」只部分通过。

### 4.5 边界阈值未校准
固定 `minimum_margin=0.08` 导致「已知支持率」随规模从 2K 的 99.48% 降到 64K 的 90.19%（V6.0）；V5.9 从 100% 降到 88.13%。报告正确指出这是准确率—覆盖率权衡，但下一版必须用独立验证集做分规模校准，并同时报告误接受率与拒答率（风险—覆盖率曲线），不能只调阈值。

### 4.6 检索仍是 O(N) 全量扫描
V6.1 的 ANN 尚未实现。512K 下 C-Former 在线检索 1.293 ms/条，反而慢于 Transformer 基线的 0.244 ms/条；这是「进入更大规模」前必须解决的结构性问题。

### 4.7 端到端链路未打通
身份解析（V6.0）与观测点问答（V2 原型）、时空递归（V5.4）、磁盘存储（V5.8）仍是相互独立的 Demo；「先身份→后观测点→再推理→证据约束解码」的完整链路要等到 V6.2/V6.5。当前没有生成式答案解码器，输出只是候选与分数。

### 4.8 少量卫生问题
- 根目录存在 `.pytest_basetemp/` 残留目录（本次评审测试产生，删除时被文件沙箱拒绝，需手动删除）；
- 峰值 CUDA 分配 2.34 GiB 已经逼近 RTX 3050 4 GB 的预算红线，后续 ANN+重排需要继续盯显存；
- 部分旧报告（TEST_REPORT.md 等）为 UTF-8 正常编码，但控制台按 GBK 阅读会乱码，建议统一声明 UTF-8 并在文档头部注明。

## 5. 建议的下一步计划（按优先级）

### P0 — 一周内：工程地基（不改算法）
1. `git init` 已有仓库 → 首次提交；按版本打 tag（v1…v6.0）；配置远端备份；`.gitignore` 确认覆盖 `artifacts/`、`*.pt`、`__pycache__/`、`.pytest_basetemp/`。
2. 建立 `pyproject.toml`（或 conda env yml）锁定 Python/torch/pytest 版本；把 README 中硬编码的 `D:\conda\...` 路径全部替换为可移植写法。
3. 根目录整顿：23 个脚本按版本归入 `scripts/vXX/` 或统一命名；把重复的 `contrastive_loss`、门控融合抽成共享模块（v60 内部至少三合一）。
4. 加 CI（GitHub Actions 或本地 pre-commit）：push 自动跑 `pytest -q`，并让默认环境也能跑测试。
5. 删除 `.pytest_basetemp/`；给 README 增加「如何新建版本」的模板说明。

### P1 — V6.0b / V6.1：外部有效性 + 检索结构
1. 建立与训练模板**隔离**的真实中文盲测集（人工或真实来源），保留错误样本——这是 V6.0 报告自己列的第一项。
2. 在独立验证集上校准 score/margin/coverage，画风险—覆盖率曲线，分规模给出阈值；冻结 2 层编码接口。
3. 实现 V6.1 分层检索：精确别名 B-tree → FTS/属性分桶 → ANN 粗召回 Top-256 → 四证据精排 Top-16；在 64K/128K/256K/512K 测 Recall@256（≥99.5%）、最终 Top-1 降幅（≤0.5pp）、内存、冷启动、p95。ANN 不达标就按既定回退条件保留「精确+FTS shard 内精确向量」混合路径，不以速度换漏召回。

### P2 — V6.2 / V6.3：端到端与时空递归
1. V6.2 打通「身份解析 → 观测点注入 → 推理 → 证据答案」；验证跨观测点身份一致性、权限泄漏率 0、跨视角证据召回 ≥95%；对象向量不得按观测点复制。
2. V6.3 把 V5.4 时空框架对齐到 V6.0 对象体系；共享递归块（1–4 跳）与直接堆 4/8 层做参数/准确率对照。

### P3 — V6.4 / V6.5：幻觉校准与集成
1. V6.4 在真实噪声下衡量幻觉与错误合并；目标 unsupported ≤2%、ECE ≤0.05、高风险错误对象自动验证率 0；同时报告 coverage，不接受单指标优化。
2. V6.5 集成原型 + 公平基线（observer-prefix Transformer、BM25/FTS+rerank、Dense RAG、关键模块消融），统一候选库/训练样本/计算预算；交付可回滚的命令与完整失败案例。

### 贯穿始终的原则（项目已写、建议坚持）
- 每个版本：3 种子 × 每规模 4–5 世界 × 固定测试集；报告均值/最小/最大与 CI；
- 记录参数、训练时间、峰值显存、磁盘、p95；
- 任何对象 ID/模板/观测点到 label 的捷径都使该轮结果作废；
- 不把合成检索成绩宣传为通用大模型能力。

## 6. 结论

这是一个**方法纪律显著高于平均水平的研究型原型项目**：版本闭环、诚实评估、安全治理、反捷径测试都做得比多数同类 Demo 认真。当前阶段的最大短板不在算法而在工程：没有版本控制、没有打包与 CI、目录复制式演进、数据仍为合成饱和集、检索仍为 O(N)。按 P0（工程地基）→ P1（真实数据 + ANN）→ P2/P3（端到端与集成）推进，项目完全有资格在 V6.5 交付一个「共享对象世界 + 受控推理层」的可运行原型与公平对照报告。
