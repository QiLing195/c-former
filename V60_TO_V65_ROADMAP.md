# C-Former 项目 V6.0—V6.5 总体研发计划

## 1. 计划目的

本路线图以 V5.9 的实测结果为起点，逐版解决当前已经确认的问题，而不是一次性堆叠功能。版本推进顺序为：

```text
V5.9 无编号对象解析
  ↓
V6.0 Token 级真实文本理解
  ↓
V6.1 分层索引与大世界检索
  ↓
V6.2 共享世界与观测点问答
  ↓
V6.3 时空对齐与受控递归
  ↓
V6.4 幻觉、冲突和开放世界边界
  ↓
V6.5 集成原型与完整对照评估
```

每个版本必须完成代码、测试、原始结果、对照报告和回滚方案。上一版没有通过质量闸门时，不进入下一版。

## 2. 当前基线与已知问题

V5.9 已完成：

- 查询不再包含对象编号或身份键；
- 名称、属性、关系、变化四证据对象解析；
- 2K—512K、每级 4 个世界的 CUDA 测试；
- 候选审核、拒绝、歧义和版本回滚；
- 512K FP16 对象缓存 62.5 MiB；
- 54 项回归测试通过。

V5.9 尚未解决：

1. C-Former 主路径仍是 Embedding、平均池化和两层 MLP，不是真正的 Token Transformer；
2. 同义词和句式仍来自受控合成词表，不能代表真实中文；
3. 在线检索仍需要分块扫描全部对象，复杂度为 `O(N)`；
4. 对象身份、观测点和回答生成还没有形成完整端到端链路；
5. 时间、空间、递归 Transformation 尚未与新对象解析器对齐；
6. 当前“未知拒绝率”和“零错误合并”只在合成集成立；
7. Transformer 对照是双编码检索模型，不是完整生成式 Transformer/LLM。

## 3. V6.5 目标架构

```text
问题文本 / 对象文档
        │
共享 Token Transformer × 4（默认，2/4/6 层消融）
        │
        ├── 精确别名与版本索引
        └── ANN 语义粗召回 Top-256
                    │
          四证据 C-Former 精排 Top-16
                    │
          身份边界与 CandidateLedger
                    │
             确认共享对象身份
                    │
       权限、时间、空间、观测点过滤
                    │
         C-Former 世界推理块 × 4
              ┌─────┴─────┐
              │ 共享递归  │ × 1—4 次
              └─────┬─────┘
                    │
        证据约束答案解码器 × 2
                    │
       answer / ambiguous / unknown /
       conflict / access_denied / stale
```

关键原则是“先身份、后观测点、再推理”：观测点不能参与对象身份猜测，只能决定确认对象的哪些事实可见、关系如何投影以及答案如何表达。

## 4. 层数与资源预算

### 默认层数

| 模块 | 默认层数 | 说明 |
|---|---:|---|
| Token Transformer | 4 | 查询和对象共享；V6.0 比较 2/4/6 层 |
| 对象证据融合 | 1 个门控模块 | 四证据共享编码，不复制四套模型 |
| C-Former 世界推理 | 4 | 对象整合、观测点、关系传播、冲突边界 |
| 共享递归块 | 2 个块重复 1—4 次 | 增加有效深度，不线性增加参数 |
| 答案解码器 | 2 | 生成答案和证据状态 |

### 建议初始配置

```text
d_model                 = 256
attention_heads         = 8
ffn_dimensions          = 768（必要时比较 1024）
token_context           = 128，V6.5 再测试 256
retrieval_dimensions    = 64 或 128
ANN coarse candidates   = 256
C-Former rerank         = 16 或 64
recursive_steps         = 1—4
```

### 硬件预算

- 当前 RTX 3050 4 GB 上，目标总参数不超过 15M；若使用预训练模型，必须同时报告总参数和实际可训练参数；
- CUDA 峰值分配目标不超过 3.2 GiB，预留驱动和桌面显存；
- 使用 FP16/BF16（硬件允许时）、梯度累积、激活检查点和动态 batch；
- 对象向量只保存一份，不随别名数、问题数或观测点数复制；
- 512K 对象向量目标：FP16 不超过 64 MiB，INT8 不超过 32 MiB。

## 5. 版本概览

| 版本 | 核心目标 | 主要解决的上一版问题 | 退出条件 |
|---|---|---|---|
| V6.0 | 真实 Token Transformer | V5.9 不理解词序、否定和真实句式 | 选出 2/4/6 层的帕累托最优配置 |
| V6.1 | ANN + V5.8 磁盘层 | 全量扫描 `O(N)`、缓存与冷启动 | 召回基本不降、延迟和常驻内存达标 |
| V6.2 | 观测点端到端问答 | 身份解析与视角问答尚未贯通 | 无权限泄漏，跨视角共享知识可验证 |
| V6.3 | 时空与递归 | 多跳变化缺少时空快照和稳定终止 | 版本、时间、循环、深度边界全部通过 |
| V6.4 | 幻觉与开放边界 | 合成集边界无法代表真实风险 | 真实未知、冲突、同名实体得到校准 |
| V6.5 | 完整集成与公平评估 | 各模块仍是分散 Demo | 形成可运行原型、完整报告和冻结基线 |

## 6. V6.0：Token Transformer 与真实文本基础

### 目标

用共享 Transformer Encoder 替换 V5.9 的平均池化主路径，使模型能处理词序、否定、修饰范围、多词别名和不同句式。

### 架构改动

1. 查询与对象使用同一个 tokenizer、embedding 和 Transformer；
2. 对象的名称、属性、关系、变化分别编码，再由证据门控融合；
3. 对比 2、4、6 层 Transformer，其他超参数和训练数据保持一致；
4. 对象身份编码仍不接收观测点、对象 ID 或评测 label；
5. 保留 V5.9 的状态机和安全 verifier。

### 训练内容

- 当前无编号组合语义数据，作为不回归集合；
- 中文同义别名、缩写、全半角、错别字、语序变换；
- 否定与范围：如“不是北方”“仅用于医疗之外”；
- 缺字段表达和多余干扰信息；
- 只差一个属性的硬负例、同名不同对象、近义但非同一对象；
- 按实体族、来源和表达模板隔离训练/验证/测试，防止模板泄漏。

### 测试矩阵

- 层数：2 / 4 / 6；
- 规模：2K / 8K / 64K；
- 每级至少 4 个世界、3 个训练种子；
- 对照：V5.9 MLP、相同层数 Transformer dual encoder；
- 指标：Top-1、Recall@10、硬负例准确率、否定准确率、歧义检出、未知拒绝、参数、训练时间、显存和 p95 延迟。

### 质量闸门

- V5.9 合成不回归集 Top-1 不低于 99%；
- 无编号检查和“改变 label 不改变编码”测试 100% 通过；
- 真实文本硬集相对 V5.9 MLP 提升至少 5 个百分点，或在准确率相差不超过 1 个百分点时显著降低边界错误；
- 6 层相对 4 层若提升不足 1 个百分点或 p95 延迟增加超过 30%，保留 4 层；
- 任何配置出现对象 ID、观测点或模板泄漏时，该结果作废。

### 交付物

`cformer_v60/`、训练脚本、2/4/6 层检查点、真实文本数据生成说明、消融结果和 `V60_REPORT.md`。

## 7. V6.1：分层索引、磁盘对象库与规模扩展

### 目标

把 V6.0 语义编码器接入 V5.8 磁盘存储，消除在线全量扫描，并保持一对象一向量。

### 检索链路

```text
精确别名 B-tree（命中即返回）
        ↓ 未命中
FTS/BM25 或属性分桶
        ↓
ANN 向量粗召回 Top-256
        ↓
四证据 C-Former 精排 Top-16
        ↓
score + margin + coverage 边界判断
```

### 工程内容

- 比较 HNSW、IVF-PQ 或可在本机稳定运行的等价 ANN；
- 对象向量 FP16/INT8 两种格式，索引与正式对象版本绑定；
- 新对象追加、删除墓碑、增量建索引、快照切换和失败回滚；
- 热缓存只保留高频 shard，不加载全部别名文本；
- 记录冷启动、热查询、批查询和并发查询。

### 测试矩阵

- 规模：64K / 128K / 256K / 512K，条件允许扩至 1M / 2M；
- 每级 4 个世界；
- 对照：精确全量扫描、V5.8 FTS、纯 ANN、ANN + C-Former；
- 指标：ANN Recall@256、最终 Top-1、索引磁盘、常驻内存、冷启动、p50/p95/p99、增量更新时间。

### 质量闸门

- ANN Recall@256 不低于 99.5%；
- 相对精确扫描，最终 Top-1 下降不超过 0.5 个百分点；
- 512K 对象向量缓存满足 FP16 ≤64 MiB 或 INT8 ≤32 MiB；
- 1M 条件下 GPU p95 目标小于 10 ms；若 ANN 在 512K 尚无速度优势，应以更大规模和 CPU/mmap 场景决定是否保留；
- 增量失败后能够回到上一可用快照，正式别名不被候选自动污染。

### 回退条件

ANN 召回不达标时，保留“精确别名 + FTS shard + shard 内精确向量”的混合路径，不以速度换取不可控漏召回。

## 8. V6.2：共享对象世界与多观测点问答

### 目标

正式验证项目的核心假设：所有信息存放在共享世界中，同一对象可以被不同问题、不同观测点复用；观测点像坐标或索引，而不是新的知识副本。

### 架构改动

1. 对象解析完成后才注入 observer embedding；
2. 可见性、权限、空间方向和角色立场使用确定性 mask；
3. C-Former 在合法 Top-K 对象和事实中做观测点调制；
4. 输出必须带对象、事实、关系和版本证据 ID；
5. 同一对象的基础向量不因观测点数量增加而复制。

### 数据与问题

- 同一共享世界中的事实查询、关系查询、比较、反向关系和跨对象组合；
- 同一问题不同观测点应产生不同但不矛盾的合法答案；
- 不同问题从不同观测点复用先前对象信息；
- 无权限事实、不可见对象、边界外关系和恶意观测点提示；
- 默认观测点与自定义观测点对照。

### 层数消融

对 C-Former 世界推理层测试 2 / 4 / 6 层。默认角色为：

1. 对象与事实聚合；
2. 观测点和权限投影；
3. 关系与 Transformation 传播；
4. 冲突和边界整合。

### 质量闸门

- 身份解析结果在合法观测点之间保持一致；
- 权限泄漏率为 0；
- 需要共享知识的跨观测点问题证据召回率不低于 95%；
- 答案中的关键断言可追溯证据比例不低于 95%；
- 4 层相对 2 层必须在复杂关系或边界集上产生明确收益，否则保留较浅配置；
- 不允许通过为每个观测点复制一份对象缓存来提高成绩。

## 9. V6.3：时间、空间对齐与受控递归

### 目标

把先前递归与时空原型接入当前对象体系，使“对象在什么时间、什么空间、经过哪些变化”成为正式查询条件。

### 数据结构

- `ObjectIdentity`：稳定身份；
- `ObjectState(object, valid_time, ingest_time, region, version)`；
- `Transformation(source, target, precondition, effect, evidence)`；
- `ObserverFrame(time, region, permissions, viewpoint)`；
- 查询固定 `world_version + ingest_cutoff + valid_time` 快照。

### 递归设计

- 使用 2 个共享 C-Former 递归块，重复 1—4 次；
- 每跳记录对象、关系、时间、空间和证据；
- 强制 cycle detection、depth limit、time monotonicity、version pinning；
- 递归期间世界版本改变时返回 `version_changed`，不继续混合快照。

### 测试内容

- 1 / 2 / 4 / 8 跳 Transformation；
- 环路、回边、错误时间顺序、跨区域不可达、未来事实泄漏；
- 递归期间增量写入、对象合并、别名回滚；
- 共享参数递归与直接堆叠 4/8 层对照。

### 质量闸门

- 循环、深度、版本变化和权限终止测试 100% 通过；
- 不允许使用查询时间之后或 ingest cutoff 之后的事实；
- 1—4 跳任务成功率目标不低于 90%，5—8 跳单独报告而不掩盖衰减；
- 共享递归相对直接堆层，在相近准确率下减少至少 30% 推理层参数；
- 每个答案能够重放完整路径，不能只返回不可解释的最终向量。

## 10. V6.4：幻觉、冲突、校准与开放世界边界

### 目标

从“能找到答案”转向“知道什么时候不能回答”，并在真实噪声下衡量幻觉和错误对象合并。

### 边界类型

- 世界中不存在该对象；
- 表达可能对应多个同名对象；
- 对象存在但问题所需事实缺失；
- 多来源事实相互冲突；
- 事实过期或版本不一致；
- 观测点没有权限；
- 递归路径不完整或超过深度；
- 检索到了近似对象但关键属性不同。

### 机制

- 用验证集校准 score、margin、coverage 和 evidence completeness；
- 比较温度缩放、分桶阈值和小型校准头；
- 输出置信区间和失败原因，不只输出单一分数；
- 引入支持/反驳双通道证据；
- 保持“模型只能 supported，外部审核才能 verified”的责任边界；
- 所有验证、拒绝、回滚和冲突解决进入审计日志。

### 对照与指标

- 对照：标准 Transformer、Evidence-RAG、无边界 C-Former、固定阈值、校准阈值；
- 指标：支持答案准确率、unsupported answer rate、false merge、unknown recall、ambiguous recall、ECE、Brier score、审核量、错误回滚恢复率。

### 质量闸门

- 未支持回答率目标不高于 2%；
- 高风险错误对象自动验证率必须为 0；
- 未知和真实歧义召回率目标不低于 95%；
- ECE 目标不高于 0.05；
- 所有正式身份变化都可以按版本恢复；
- 若降低幻觉只能依靠大幅拒答，必须同时报告 coverage，不允许只报告低幻觉率。

## 11. V6.5：集成原型、真实任务和冻结基线

### 目标

形成一个可以从真实文本输入到带证据回答的统一 Demo，并明确项目相对 Transformer/RAG 的实际优势和不适用范围。

### 集成范围

- 4 层共享 Token Transformer；
- 精确别名 + FTS/属性 shard + ANN Top-256；
- 四证据 C-Former 精排；
- CandidateLedger 和版本化对象库；
- 观测点、权限、时间、空间过滤；
- 共享参数递归 Transformation；
- 两层证据约束答案解码器；
- 完整日志、证据引用和回滚命令。

### 最终数据集

1. 合成压力集：保持历史版本可比性；
2. 中文对象别名集：真实别名、缩写、口语、错别字和描述性指代；
3. 共享世界问答集：不同观测点、不同问题复用同一对象知识；
4. 时空递归集：版本、有效时间、空间和多跳变化；
5. 安全边界集：未知、歧义、冲突、权限和证据不足；
6. 分割必须按实体族、来源、时间和模板隔离，并公开泄漏检查结果。

### 公平基线

- 4 层参数相近 Transformer；
- 标准 Transformer observer-prefix；
- BM25/FTS + Transformer reranker；
- Dense RAG / Evidence-RAG；
- C-Former 各关键模块消融。

所有模型使用相同候选库、相同训练样本、相同计算预算和相同边界控制器；同时报告总参数、可训练参数、索引大小和缓存，不能只比较准确率。

### 最终验收

- 真实对象解析 Top-1 不低于公平 Transformer 基线 1 个百分点以上；若相同，则必须在参数、缓存或边界指标上形成清晰优势；
- 相对基线，真实歧义/冲突的安全处理至少提升 5 个百分点；
- 权限和未来事实泄漏率为 0；
- 答案证据可追溯率不低于 95%；
- 512K 与至少一个更大规模完成 4—5 世界测试；
- 完整回归、原始 JSON、检查点、失败案例和限制说明齐全；
- 明确声明该系统是“共享对象世界与受控推理层”，不把合成检索成绩宣传为通用大模型能力。

### V6.5 冻结产物

```text
cformer_v65/
train_v65.py
evaluate_v65.py
artifacts/v65_*.json
V65_ARCHITECTURE.md
V65_BENCHMARK_REPORT.md
V65_FAILURE_CASES.md
V65_REPRODUCIBILITY.md
```

## 12. 全阶段统一测试规则

每个版本统一执行：

1. 单元测试和旧版本回归；
2. 至少 3 个训练种子；
3. 每个规模 4—5 个固定世界；
4. 固定测试集不得参与阈值或早停选择；
5. 报告均值、最小值、最大值，关键版本增加置信区间；
6. 记录参数、训练时间、峰值显存、磁盘、常驻内存和 p95 延迟；
7. 保存失败案例，不只保存总体分数；
8. Transformer/RAG 对照必须使用同一数据和候选；
9. 任何对象 ID、模板、观测点到 label 的捷径都会使该轮实验无效；
10. 自动写入正式对象或别名的数量必须单独报告。

## 13. 关键风险与停止规则

| 风险 | 识别方式 | 处理或停止规则 |
|---|---|---|
| 层数增加但无收益 | 2/4/6 层消融 | 提升不足 1 点且延迟增加明显时保留浅层 |
| 合成数据饱和 | Top-1 长期 100% | 不再扩大同类合成集，转向真实语言和边界集 |
| ANN 漏召回 | Recall@256 下降 | 回退 shard 内精确检索，不牺牲关键对象 |
| 观测点泄漏身份 | 改变 observer 后 identity 改变 | 强制身份解析前移，删除该路径训练结果 |
| 固定阈值导致大量审核 | 支持率随规模下降 | 分规模/分类型校准并报告审核量曲线 |
| 递归失控 | 环路、版本混合、深度爆炸 | 确定性控制器优先于神经网络继续推理 |
| 参数重新膨胀 | 每版总参数增长 | 共享编码器、共享递归、低秩适配；无收益模块删除 |
| 幻觉率低但拒答过多 | coverage 明显下降 | 同时约束准确率、风险和 coverage，不接受单指标优化 |

## 14. 推荐执行顺序

当前立即执行 V6.0，不同时开启 V6.1—V6.5 的实现。V6.0 首先建立真实中文小数据集和 2/4/6 层 Token Transformer，在 2K、8K、64K 完成对照；确认 4 层是否值得后，再冻结文本编码接口进入 V6.1。

建议每版都沿用以下循环：

```text
设计 → 最小实现 → 单元测试 → 小规模闸门
     → 4世界梯度测试 → 对照与消融 → 失败修正
     → 冻结接口与报告 → 下一版本
```

