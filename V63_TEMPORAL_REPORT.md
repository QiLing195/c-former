# C-Former V6.3-M0 时间轴（as-of 快照）报告

日期：2026-08。范围：路线图 §9 的时间轴最小前置——`as_of` 快照查询与未来事实
泄漏防线。递归 Transformation、ingest/valid 双时钟不在本测内。

## 1. 实现

| 组件 | 说明 |
|---|---|
| `parse_as_of` | 词法识别时间切点："截至/截止/到 YYYY 年(为止)"、"YYYY 年的时候"；版本号（Qwen2.5）不误触发 |
| `WorldReasoner.select(as_of=…)` | 候选先按 `year ≤ as_of` 过滤再取极值；trace 记录 `as_of=…;filtered_future=N` |
| `TemporalNoMember` | 过滤后为空的显式结论：unknown + 不回退神经路径 + 不进 ledger 提案 |
| pipeline 挂接 | 自动从查询文本解析 as_of 并传入推理块；空集结果带 `temporal_no_member_as_of=YYYY` |

## 2. 真值构造（零人工标注）

对每个系列的每个历史年份切点 Y：查询"截至{Y}年，{company} 的 {series} 系列最新模型是什么？"，
期望 = 年份≤Y 中最大者（平局取系列内定义顺序）；共 **57 条 as-of 查询**。
另生成 **36 条空集查询**（截至最小年-1），期望显式 unknown。

## 3. A/B 结果（3 种子一致；`artifacts/v63_temporal_results.json`）

| 指标 | naive（忽略 as_of） | temporal（快照过滤） |
|---|---:|---:|
| **未来事实泄漏率** | **100%** | **0%** ✅闸门 |
| as-of Top-1 | — | **100%** ✅≥95% |
| 空集显式拒绝率 | — | **100%** ✅ |
| 留出集回归 | 100% | 100%（无损失） |

naive 臂的 100% 泄漏说明：无时间防线时，"2021 年最新的 GPT"会被答成 GPT-5.4——
检索层天然偏向新对象（证据相似度最高），这不是概率性错误而是结构性必然，
因此防线必须是确定性的先验过滤，而不是阈值或训练能解决的。

## 4. 边界

1. 年份来自证据文本正则抽取；证据缺年份的对象不参与时间裁决（回退原路径）；
2. 只有 valid_time（发布年）语义；ingest_time / 版本固定 / 递归 Transformation 未实现；
3. 时间词表有限（"截至/截止/到…为止/…年的时候"）；更多表述需扩词表或意图分类。

## 5. 下一步

1. ingest/valid 双时钟与版本固定查询（§9 正式结构）；
2. Transformation 递归（1–8 跳）+ cycle/depth 控制器；
3. 别名表指代消解 + 数据扩量后复验全部既有闸门。
