# C-Former V3 分层记忆规模测试

测试日期：2026-07-15  
环境：Python 3.13、PyTorch 2.9 CPU

## 优化目标

V2 对全部记忆做稠密查询。V3 改成：

```text
完整原始事实
    -> 48 维精确事实表示 + 32 维低成本索引
    -> Observer + Question 第一阶段检索
    -> 吸收第一跳证据
    -> 第二阶段检索
    -> 两条候选事实做精确注意力
    -> 答案
```

所有原始事实仍然保留；Top-K 只是决定哪些事实进入昂贵推理层，不代表永久删除其他信息。

## 实验设置

| 项目 | 配置 |
|---|---|
| 人物 | 64 |
| 物品 | 32 |
| 地点 | 16 |
| 世界规模 | 128 / 512 / 2,048 条事实 |
| 固定测试世界 | 每个规模 5 个，共 15 个 |
| 每个世界问题 | 36 |
| 每个规模测试问题 | 180 |
| 总测试问题 | 每个模型 540 |
| 训练规模 | 128 和 512 |
| 2,048 规模 | 只用于规模外推评估 |

两个模型都使用合计 3,000 个更新步骤。训练随机生成世界，固定测试世界由确定种子生成。它们与训练使用相同结构分布，不是自然语言跨分布测试。

## 对照模型

| 模型 | 参数量 | 查询方式 |
|---|---:|---|
| Dense Transformer | 67,136 | 每个问题重新编码全部事实、观测点和问题 |
| Hierarchical C-Former | 90,370 | 世界编码一次，低维索引扫描，两阶段精确检索 |

Dense 基线共享事实、问题和观测者 token embedding，避免人为削弱身份匹配。C-Former 比 Dense 多约 34.6% 参数，并使用两阶段证据位置作为辅助监督；因此结果体现的是“架构 + 检索监督”的整体收益，不是纯参数等量对比。

## 准确率

### 总体

| 事实数 | Dense Transformer | Hierarchical C-Former |
|---:|---:|---:|
| 128 | 5.56% | 100.00% |
| 512 | 5.56% | 98.89% |
| 2,048 | 5.00% | 98.33% |

该任务按类别空间加权后的随机基准约为 5.21%。Dense 基线在 3,000 步内仍接近随机水平，训练损失下降但没有学会稳定的算法式实体检索。

### C-Former 分任务

| 事实数 | 两跳真实位置 | 观测者认知 | 持有者 | 观测点外信息 |
|---:|---:|---:|---:|---:|
| 128 | 100% | 100% | 100% | 100% |
| 512 | 100% | 95.0% | 100% | 100% |
| 2,048 | 100% | 92.5% | 100% | 100% |

随着世界扩大，错误全部集中在高密度的观测者认知事实；客观事实、两跳推理和观测点外信息保持 100%。

## 证据召回

当前精确结构化任务每阶段选择 Top-1，第二阶段可以同时使用两轮候选：

| 事实数 | 第一跳 Recall@1 | 第二跳 Recall@2 |
|---:|---:|---:|
| 128 | 100.00% | 100.00% |
| 512 | 99.44% | 99.44% |
| 2,048 | 98.33% | 98.33% |

曾测试每阶段 Top-2、4、8、16。Top-8 在 2,048 事实上的总体准确率只有 91.11%；改为 Top-1 后达到 98.33%。原因是多个相似 `BELIEF` 事实同时进入精确注意力时会互相干扰。自然语言任务存在歧义，不能直接固定 Top-1，应使用动态阈值或重排序。

## 查询延迟

同一个世界连续回答一批 36 个问题：

| 事实数 | Dense 重编码 | C-Former 缓存查询 | 加速比 |
|---:|---:|---:|---:|
| 128 | 15.29 ms | 4.46 ms | 3.4× |
| 512 | 107.53 ms | 4.15 ms | 25.9× |
| 2,048 | 945.98 ms | 3.94 ms | 240.1× |

Dense 时间随事实量快速上升，符合全局自注意力的二次复杂度趋势。C-Former 只做低维线性扫描和两个事实的精确推理，在本规模内主要受固定框架开销影响。延迟来自小型 CPU Demo，绝对数值不能直接外推到大模型。

## 缓存大小

这里统计 48 维精确表示和 32 维索引，不包含原始事实存储、模型权重和运行时临时张量：

| 事实数 | FP32 缓存 | FP16 估算 |
|---:|---:|---:|
| 128 | 0.039 MB | 0.020 MB |
| 512 | 0.156 MB | 0.078 MB |
| 2,048 | 0.625 MB | 0.313 MB |

事实数扩大 16 倍时缓存也扩大 16 倍，保持线性增长。Dense 基线没有持久世界缓存，但每次问题都产生更大的临时注意力计算；本实验没有可靠测量进程峰值内存，因此不报告显存/RAM 加速比。

## 结果解释

### 得到支持的判断

1. 分层索引显著改善大量同类事实中的可训练性。
2. 模型在只训练到 512 条事实后，可以外推到 2,048 条事实并保持 98.33%。
3. 观测点之外的信息和两跳客观推理没有随世界扩大而退化。
4. 缓存按事实数量线性增长，不再要求所有事实进入二次复杂度注意力。
5. 同一世界多问题查询的速度优势随世界规模扩大而增加。

### 必须保留的限制

1. Dense Transformer 理论上能够表达这些函数；本结果只说明在当前模型大小、训练预算和纯答案监督下没有学会。
2. C-Former额外使用了证据位置监督，监督信息比 Dense 更强。下一步应加入带相同证据监督的 RAG/Transformer 基线。
3. 事实是结构化 token，不是自然语言。
4. 2,048 条事实仍远小于真实大型知识库。
5. Top-1 对精确结构化关系有效，但可能漏掉自然语言中的多证据答案。
6. 当前缓存仍全部放在同一设备；尚未实现 GPU 索引、CPU 原文和磁盘历史的三级存储。

## 结论

V3 解决了“世界扩大后缓存和注意力成本失控”的第一阶段问题：缓存线性增长，精确推理只处理两阶段候选，2,048 条事实保持 98.33% 准确率。相比稠密拼接 Transformer，优势主要来自可复用世界表示、显式检索归纳偏置和证据监督，而不是 Transformer 无法表达相同答案。

下一步最公平的测试应是：Hierarchical C-Former 对比“相同检索监督的 Transformer/RAG”，并把事实改写为自然语言、加入三跳问题和动态 Top-K。

