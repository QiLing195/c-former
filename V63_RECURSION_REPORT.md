# C-Former V6.3-M0 Transformation 递归报告

日期：2026-08。范围：路线图 §9 的递归最小原型——确定性多跳行走 + 安全控制器，
以及顺带的别名指代消解。ingest/valid 双时钟按优先级评估后**推迟到数据扩量时实现**
（当前单快照数据无法支撑多版本语义，见 README 优先级讨论）。

## 1. 实现

| 组件 | 文件 | 说明 |
|---|---|---|
| `TransformationGraph` | `cformer_v63/recursion.py` | 从「关系」证据的"前一代是 P"零标注构建 predecessor/successor 图 |
| `walk` 控制器 | 同上 | visited 集环检测、`MAX_HOPS=8` 深度上限、链尾显式终止——全部确定性 |
| `MultiHopResolver` | 同上 | 词法解析"{对象} 的前一代 / 往前(后)数N代"，中文数字与阿拉伯数字均支持 |
| pipeline 挂接 | `cformer_v61c/pipeline.py` | 精确别名之后、神经编码之前；`ok→SUPPORTED(path=recursion)`，`chain_end/cycle/depth_limit→显式 UNKNOWN`，全程带路径轨迹 |
| 别名指代消解 | `cformer_real.apply_aliases` + 数据集别名词表 | "千问/通义→Qwen"、"谷歌→Google"等约 30 条；接入词法锚定 |

## 2. 结果（真值沿链程序化生成，共 1490 条；`artifacts/v63_recursion_results.json`）

| 跳数桶 | 方向 | n | Top-1 | 支持率 |
|---|---|---:|---:|---:|
| 1—4 跳 | 后退 | 524 | **97.3%** ✅≥90% | 99.6% |
| 1—4 跳 | 前进 | 524 | **97.3%** ✅≥90% | 100% |
| 5—8 跳（单独报告） | 后退 | 221 | 94.6% | 100% |
| 5—8 跳（单独报告） | 前进 | 221 | 94.6% | 100% |
| 别名冒烟："千问系列最新" | — | — | 正确命中 qwen3-7-max ✅ | |
| 留出集回归（3 种子） | — | — | 100% 无损失 ✅ | |

cycle 检测与深度上限由合成环图单元测试覆盖（`tests/test_cformer_v63_recursion.py`，9 项）。

## 3. 未达满分的失败分析（诚实记录）

~2.7% 的 Top-1 缺口集中在两类：
1. **锚定子串歧义**：成员名互为前缀时（如 Claude 家族密集命名）最长匹配偶尔选错起点；
2. **跨年平局链序**：同年多成员系列的行走序依赖定义顺序，个别查询措辞使锚定名与
   链上预期位置错位。
两者都是词法锚定的已知边界，扩量后可通过别名表补全与锚定消歧规则收敛。

## 4. 边界声明

1. 本原型是**结构递归语义**（图行走），不是路线图中"共享参数神经递归块"——后者属
   V6.5 集成时的神经架构对照项（共享递归 vs 直接堆层的参数效率比较）；
2. Transformation 目前只有 predecessor/successor 一种关系；precondition/effect/
   跨区域不可达等 §9 完整语义待数据模型扩展；
3. 5—8 跳 94.6% 与 1—4 跳差距小是因为行走确定性相同，缺口同源于锚定而非跳数衰减。

## 5. 下一步

1. ingest/valid 双时钟（绑定 1K+ 多版本数据扩量）；
2. V6.4 幻觉校准：真实噪声下的 unsupported/ECE/误合并（需要真实冲突证据数据）；
3. 锚定消歧规则 + 别名词表扩充，把 1—4 跳推向 100%。
